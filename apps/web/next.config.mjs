/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // @gharsetu/shared ships compiled dist/ already, so no transpilePackages needed.
};

export default nextConfig;
