# Prototype change log

Per CLAUDE.md Working rule #9 — the static prototype (`prototype/`) is kept in sync with the live app. This file records prototype-affecting changes going forward so the eventual app port has a single running list. Each entry links the per-feature planning file in [`docs/planning/features/`](./features/), which holds the detailed rationale, IA, and test cases.

> Earlier prototype work (Phases 1–8 and the v8 build-out through 2026-05-26) is documented in the individual feature planning files and the `agent-team-change-logs/`; this running list starts 2026-05-27.

| Date | Area | Change | Planning file | App-port carry-over |
|------|------|--------|---------------|---------------------|
| 2026-06-02 | Super Admin · Invoices · reconcile to real orgs | Audit found the invoices page billed a **different org set** than the platform (Triline/Himalaya/etc. vs the real 8). Replaced the 25 hand-written rows with a **JS roster generator** driven by the platform's **5 approved orgs** (pending orgs aren't billed): Delhi Dwellings (Premium), Cityscape + Green Valley (Standard), Urban Nest + Sunrise (Basic). 18 invoices Jan–May (13 paid / 4 issued / 1 cancelled — Sunrise's deactivation month), amounts ₹999/₹2,999/₹6,999, newest-first. Org filter options updated to the 5; month dropdown gained Jan. | — | Server returns invoices for approved orgs only; statuses derived. |
| 2026-06-02 | Masters (9 pages) · Add/Edit bug fix | **Add** built rows **without the leading `<td data-pg-serial>`** → every value shifted under the wrong header; **Edit** only toasted, never updating the row. Fixed across all 9 masters — super-admin cities/states/business-types/payment-methods + admin amenities/categories/property-types/specializations/visit-purposes: add now prepends the serial cell (+ `Paginator.refresh`), and `submitEditForm` writes the edited values back to the row (via an `editingRow` lookup by `data-name`) and re-syncs the Edit link. | — | Server CRUD returns the persisted row; client re-renders the table. |
| 2026-06-02 | Super Admin · Payment Methods · "Reference required" column | New **Reference required** column (Yes/No) + Add/Edit checkbox; rows carry `data-ref`. Cash = No, others = Yes. Added `refRequired` to single-source `assets/payment-methods.js`. Wired **super-admin invoice-detail Mark Paid**: when the chosen method has `refRequired:false` (Cash) the reference field is **hidden and not required**; otherwise shown/required (label+placeholder still swap per method). | — | `payment_methods.reference_required` boolean; Mark-Paid validation reads it. |
| 2026-06-02 | Admin · Visit Purposes · "Requires details" column | New **Requires details** column (Yes/No) + Add/Edit checkbox; rows carry `data-detail` (Other + Maintenance vendor = Yes). Lets a purpose be flagged so a free-text detail input is shown alongside it when a visit is logged. (Consuming visitor-form wiring is a follow-up.) | — | `visit_purposes.requires_detail` boolean; visitor form shows a detail input when set. |
| 2026-06-02 | Super Admin · Business Types · remove "Other" | Removed the **Other** business type from the master **and** from the shared `assets/business-types.js` (so org sign-up stays consistent) — only the real types remain (PG/Hostel, Housing Society, Individual Landlord, Property Management Firm, Co-living). | — | Seed only the real business types; no catch-all "Other". |
| 2026-06-02 | Admin · Organization · invoices filters + Plan column | Removed the meaningless **Issued from/to** date filter (single-org view; user couldn't pick past dates and it had no purpose); cleaned the date logic out of `filterOrgInvoices`. Added the missing **Plan** column to the invoices table (Standard for this org, matching its plan history) so the Status + Plan filters are visible/usable. | — | Org invoice view: columns incl. plan; filter by status/plan. |
| 2026-06-02 | Admin · Delegations · "All" tab single table | Clicking **All** previously stacked all four status panels (4 separate card-tables = "different sections"). Added one combined `#dpanel-all` table; `switchDelegTab('all')` now clones the rows from the four panels into it (re-serialised), showing a single unified list. Per-status tabs unchanged. | — | App: one delegations list with status tabs/filters over a single query. |
| 2026-06-01 | Admin · sidebar label · Rent → Rent collection | Renamed the admin sidebar nav item **"Rent" → "Rent collection"** across all 25 admin pages (20 top-level + 5 master-data subpages; `href="rent.html"`/`"../rent.html"`, `class="sidebar-link"`). The mobile bottom **tabbar keeps the short "Rent"** label (space-constrained, like "Maint." for Maintenance). `rent.html` page title unchanged. | — | App-port: nav config label "Rent collection"; route stays `/admin/rent`. |
| 2026-06-02 | Admin · Check Availability · checkbox filters + range buckets + show occupied | (1) **Bedrooms / Bathrooms / Kitchen → checkbox lists** (multi-select, match-any) replacing the min `<select>`s / kitchen select. (2) **Rent & Area → grouped range checkboxes** (Below ₹10,000 · ₹10,000–₹15,000 · ₹15,000–₹20,000 · ₹20,000–₹30,000 · Above ₹30,000; Below 500 · 500–800 · 800–1,000 · Above 1,000 sq ft) instead of open-ended min/max inputs — match-any union. Generic `passGroup(checked, buckets, it)` engine + `renderChecks()`. (3) **Occupied inventory now shown** (fix: page used to hide it, contradicting the wizard which knew the unit was occupied). Added the real occupied units/rooms (101,102,10501,10502,20102,20203) + ~⅓ of generated units occupied → **72 cards (48 available / 24 occupied)**; cards show a real **Available** (green) / **Occupied** (grey) badge + "frees <date>"; new **Status** filter (Available/Occupied); the details popup badges status and **disables Create Lease for occupied** ("Occupied — can't lease"). Count label now "N results" (not "available"). | [`2026-06-01-check-availability.md`](./features/2026-06-01-check-availability.md) | Port: filters → `?bed[]=&bath[]=&kitchen[]=&rent[]=&area[]=&status[]=`; availability = real `status` + free-from date; occupied excluded from lease creation server-side (BL-01). |
| 2026-06-02 | Super Admin · Plans · "View list" filter fix + Standard org count | (1) **Bug:** the per-plan **"View list →"** linked `organizations.html?plan=<id>` (numeric 1/2/3), but the org list only accepts plan **slugs** (`basic|standard|premium`) → filter silently dropped, showed all. Fixed to `?plan=<slug>`. (2) **Data:** `plans.js` Standard `orgs` 5 → **3** to match the actual org list (basic 4 / standard 3 / premium 1) and the reconciled dashboard donut. | — | Port: deep-link by plan slug; plan org-counts are aggregates of the org table. |
| 2026-06-02 | Super Admin · Invoices · month filter (+ org filter) now work | **Bug:** the From/To **month** selects had **no `onchange` handler** and rows carried no month attribute → did nothing. Fix: a script derives **`data-month` (YYYY-MM)** from each invoice number (`INV-YYYY-MM-…`) and wires both selects to a **range predicate** via `Paginator.setPredicate` (AND-combines with the tile/org/plan filters); defaults set to **Any → Any** (was May→May, which would hide non-May rows on load). Same root cause fixed for the **Organization** filter — rows had no `data-org`, so it's now derived from the Org cell (matches the option text). | — | Port: filter by `?from=&to=&org=`; server returns the scoped invoice set. |
| 2026-06-02 | Super Admin · Invoices · remove duplicate KPI strip | Removed the top KPI grid (Total Issued / Outstanding / Paid This Month / Cancelled This Month) — it duplicated the filter-tile row directly below it (All / Issued / Paid / Cancelled with live counts). Filter tiles retained. | — | Port: one summary row (the filter tiles carry counts); no separate KPI strip. |
| 2026-06-02 | Admin · Check Availability · type-checkbox filter + load-on-scroll + moved to Leases | (1) **Property type filter → checkbox list** (multi-select, match-any; renders from `GHARSETU_PROPERTY_TYPES`), replacing the single `<select>`. (2) **Load-on-scroll** — results render 9 at a time; an `IntersectionObserver` on a `#availMore` sentinel appends the next batch as it nears the viewport (no full dump). (3) **More mock data** so the behaviour is real: 5 extra properties (varied type/city/state) + a deterministic generator → **66 cards** (59 units / 7 rooms), 8 batches; 4 property types, 4 cities. (4) **Entry button moved Properties → Leases** per user (better home) — removed from `properties.html`, added beside "+ New Lease" on `leases.html`; the page's sidebar/more-sheet active state + back-link re-pointed Properties → **Leases**. (Sticky-desktop filter rail + mobile slide-in "Filters" sheet were already built.) Generated ids (9xxx) aren't in create-lease → their deep-link opens the wizard normally (graceful). | [`2026-06-01-check-availability.md`](./features/2026-06-01-check-availability.md) | Port: type filter = `?type[]=`; load-on-scroll = server pagination/infinite query; entry from the Leases screen. |
| 2026-06-02 | Super Admin · Dashboard · reconcile all data to org list | The dashboard's hardcoded figures contradicted `organizations.html` (8 orgs: 4 active / 3 pending / 1 deactivated; users 42/12/4/8 active). Fixed **everything to match**: tiles **Organizations 8** (4 active · 3 pending · 1 deactivated), Pending **3** (Oldest 21/05/2026), **Total Active Users 66** (Σ active-org users), **Cap Utilization 53%** (24/45 capped seats — relabelled from the unverifiable "Plan Coverage 87%"); **Plan Distribution donut** recomputed to all 8 orgs (Basic 4 / Standard 3 / Premium 1, centre "8", arcs re-proportioned); **Recently Approved** now lists the 4 *actually-active* orgs (Urban Nest / Green Valley / Cityscape / Delhi Dwellings, correct plans + ids + approval dates) — removed pending Saffron Stays which was wrongly shown Active; **Recent Platform Activity** rewritten coherently (Metro/Greenleaf/Saffron = sign-up received; Sunrise deactivated; Urban Nest approved). | — | Port: dashboard KPIs + charts are aggregates of the same org dataset — never hardcoded; compute server-side. |
| 2026-06-02 | Super Admin · Dashboard · remove pending alert + fix tile links | Per user: **removed the "3 organizations awaiting your review" alert card** (redundant with the Pending tile). Fixed the KPI tiles to deep-link to the correct filtered view — **Total Active Users** now → `organizations.html?filter=active` (was generic `organizations.html`); Pending Sign-ups → `?filter=pending` and Plan Coverage → `plans.html` already correct. `organizations.html` already honours `?filter=pending\|active\|deactivated\|all` (applies the matching tile after Paginator init), so all four tiles now navigate to the right card. | — | Port: dashboard KPIs deep-link with `?filter=` to the org list; one shared query convention. |
| 2026-06-02 | Public · Privacy & Terms · remove blue card, uniform grid | Per user: the empty **navy "feat panel"** decorative block in the Overview card (`legal-feat-panel`, injected by `legal.js` only on the featured card) made the page look uneven. **Removed it** and flattened the legal layout from the 12-col bento (oversized col-8 Overview + col-4 right stack + col-6/col-12 spans) to a **uniform 2-col card grid** (1-col ≤1023px) so cards size to content and flow/merge evenly; dropped `height:100%`, accent alternates. Affects both `privacy.html` + `terms.html` (shared `renderLegalDoc`). Removed unused `.legal-feat-panel` / `.legal-col-*` / `.legal-right` / `.legal-card.feat` CSS. Trust strip unchanged. | [`2026-06-01-public-pages-redesign-v2.md`](./features/2026-06-01-public-pages-redesign-v2.md) | Port: legal docs render as a uniform responsive card grid; content from `legal.js`/CMS. |
| 2026-06-02 | Admin · NEW Check Availability page (search → lease) | **NEW `admin/check-availability.html`** reached from a **Check Availability** button beside Add Property on `properties.html`. Left **filter rail** + center **result cards** of available units/rooms. Filters: Looking-for (Both/Units/Rooms), Available-from (date), Property (searchable, rule #18), State→City cascade, Property type, Bedrooms/Bathrooms (min), Kitchen, Rent range, Area range, Amenities (match-all) — facets from single-source masters (`property-types.js`, `locations.js`, NEW `assets/amenities.js`). Cards lead with Property · Unit (· Room) + type chip + Available badge + rent + "Available from"; click → **details popup** (full attribute summary + amenities) → **Create Lease** reuses the existing `create-lease.html?unitId=&roomId=` deep-link (lands Step 4). Numeric-id `PROPERTIES/UNITS/ROOMS` mock matches create-lease so deep-links resolve. **No-double-count:** room-based units list their rooms, never also as a parent-unit card. Mobile: filters collapse to a slide-in sheet. NEW `.avail-*` CSS (composes tokens). Verified: 5 unit + 7 room cards, all deep-link ids resolve, script parses. | [`2026-06-01-check-availability.md`](./features/2026-06-01-check-availability.md) | Port: `GET /units/availability?…` returning only inventory free for the window (BL-01/BL-18), org+role scoped; facets from masters; Create Lease → wizard owns dates/consent. |
| 2026-06-01 | Admin · Rent Collection · tiles + query-time status model + Portfolio tab | **Major completion pass on `admin/rent.html`.** (1) **Top KPI tiles** → **Total Due · Collected · Outstanding · Overdue** (Total Due = Collected + Outstanding by construction; Overdue is a **count** + "N leases past due", not ₹). (2) **Payment status is now computed at query time, never stored** — new `computeStatus(due, collected, dueDay)` derives **Paid / Partially Paid / Outstanding / Overdue / Prepaid** (precedence: prepaid > paid > overdue[past due-day+5] > outstanding[unpaid, in grace] > partially-paid[in grace]). ROSTER dropped the stored `ms` status, now carries only `mp` (collected) + `dd` (due day); months aligned to **June 2026** (`TODAY_DAY=12`, `GRACE_DAYS=5`). Badge labels + monthly filter tiles updated (All/Paid/Partially Paid/Outstanding/Overdue/Prepaid). (3) **NEW Portfolio tab** (3rd view): Portfolio Summary (Total Properties · Active Leases · Monthly Due · Collection Rate + progress bar) + Month-by-Month Trend table. (4) **Collection rate reconciled** — computed collected÷due over the **same** period and clamped ≤100%, so the cross-period **507% bug is impossible**; advance (prepaid) shown separately. Verified via DOM-stub: 5 statuses present, KPIs reconcile, trend June 80% / past 100%, script parses. | [`2026-06-01-rent-collection-completion.md`](./features/2026-06-01-rent-collection-completion.md) | App-port: status computed in the serializer from `SUM(payments for period)` vs period `amount_due` + due date (+5d grace) — never a column. Collection rate = collected/due per requested scope. Portfolio = org rollup + per-month aggregate. SRS: add "payment status is derived, not stored" NR. |
| 2026-06-01 | PM · Properties · Occupancy column removed (admin parity) | Replicated the admin Properties change onto **`pm/properties.html`**: removed the **Occupancy** column (header + 5 row cells: 88% / 94% / 80% / — / —). PM table now: # · Property · City · Units · Rooms · Tenure · Status · Action (8 cols, verified header=rows). Admin-only changes that don't map to PM were **not** copied: the Available/Total Rooms **summary tiles** (PM's tile row is tenure-status pagination, not a portfolio summary), the **edit-form off-by-one fix** (PM is operations-scoped — no Add/Edit Property modal), and the **CSV export fix** (PM has no export). The upcoming-lease date fix was already mirrored to `pm/unit-detail.html` earlier this session. | — | App-port: PM properties view = property-scoped query; same columns as admin minus org-CRUD affordances. |
| 2026-06-01 | Admin · Audit Log · "Actor" → "User" | Renamed every visible **"Actor"** to **"User"** on `admin/audit-log.html` (the records are users, so "Actor" read as jargon): table column header `Actor → User`, filter labels `Actor role → User role` and `Actor name → User name`, plus the matching element ids (`actor-role → user-role`, `actor-search → user-search`) and their `for=` attributes. No JS referenced the old ids. | — | App-port: label the audit "who" column **User**; the underlying audit_log column can stay `actor_user_id` internally. |
| 2026-06-01 | Docs · Solution Overview | **Security Guard role bullet added to the client-facing Solution Overview.** New bold-lead bullet in the Visitor Management sub-section (`VISITOR_BULLETS` in `doc-assets/templates/generate_solution_overview.js`), styled to match the SAAS section’s “New Super Admin role” bullet: **New Security Guard role** — a gate-level role that manages visitors for assigned properties and can log walk-in visitors at the gate for the tenant to approve. Regenerated `Solution_Overview.docx` (16,854 bytes); python-docx round-trip confirms the bullet + bold-lead styling. | [`2026-06-01-security-guard-role.md`](./features/2026-06-01-security-guard-role.md) | Doc-only. SRS already reflects 6 roles. |
| 2026-05-27 | Public · Home | Homepage visual redesign — glass-blur sticky nav (white → navy on scroll), navy-gradient hero with saffron mesh + browser-framed dashboard mock + factual stats strip (120+ units · 18 buildings · 4 roles), alternating two-column capability rows, three-step "how it works", bento role grid (Admin emphasized), plans on light-gray with popular-card glow, merged navy CTA/footer (footer deep-navy `#0D1757` to separate from CTA), `IntersectionObserver` scroll-reveal + reduced-motion handling, skip link. Plan cards still render from `assets/plans.js` (`renderMarketingPlans`) so home/signup/Super-Admin stay one source. | [`2026-05-27-homepage-redesign.md`](./features/2026-05-27-homepage-redesign.md) | Port to `apps/web` landing route. New utilities are landing-only (inline `<style>` in `index.html`); when porting, fold the design tokens already in `styles.css` into `tailwind.config.ts` verbatim (Scope rule I). Stats are factual — keep no fabricated testimonials/logos. |
| 2026-05-27 | Public · all | **Shared public chrome** — `assets/public-chrome.js` renders one identical sticky glass nav (Login + Register) + deep-navy Company/Legal footer into `#gs-public-nav` / `#gs-public-footer` on `index` / `contact` / `privacy` / `terms`. Auth pages (login, signup) stay chrome-free. | (this file) | Becomes a shared layout/component in `apps/web` (e.g. a `(public)` route-group layout). Login/Register links + nav-scroll behaviour port as-is. |
| 2026-05-27 | All roles · Profile | **Role** detail row removed from all 5 profile pages — role now shown only as the `.profile-role` badge. Edit-Profile modal added (Name + Mobile editable; **Email + Role locked**). | — (`2026-05-26-common-ui-cleanup.md` profile section) | App: profile PATCH allows only name + phone; email/role immutable from self-service (role change is an admin action, audited). |
| 2026-05-27 | Super Admin · Server Logs | **Lines** column removed from the log-file table (header + cell). Underlying line count retained for the file-preview modal. | [`2026-05-27-server-logs-page.md`](./features/2026-05-27-server-logs-page.md) | Log-list endpoint need not surface a per-file line count column. |
| 2026-05-27 | Super Admin · sidebar | `master-data/payment-methods.html` was missing the **Business Types** sublink — added. All 13 Super Admin surfaces now share an identical sidebar item sequence. | [`2026-05-27-business-types-master.md`](./features/2026-05-27-business-types-master.md) | One nav config drives all Super Admin pages in the app — no per-page nav drift. |
| 2026-05-27 | Masters (6) | Deactivate-blocked reason moved out of the table cell into the disabled **Deactivate** button's `title` tooltip ("Cannot deactivate — currently used by N record(s)…"). Applied to cities / states / payment-methods / business-types (platform) + amenities / categories (org). | [`2026-05-27-master-data-ownership-split.md`](./features/2026-05-27-master-data-ownership-split.md) | App: surface the in-use count via the row's deactivate affordance (tooltip / disabled-state title), not a separate cell. Deactivate still blocked while `usage_count > 0`. |
| 2026-05-27 | Admin · Dashboard | KPI "Overdue Tenants" → **"Overdue Leases"** (overdue is anchored to the lease, not a tenant). "Property Snapshot" section replaced with **"Recent Open Maintenance Requests"** (latest 4 open). | — | n/a — copy/IA tweak. |
| 2026-05-27 | Admin · Master Data | **NEW Property Types master** (org-level) — `master-data/property-types.html` + single-source `assets/property-types.js`; landing card; sublink rolled out to all 17 admin nav surfaces (sidebar + more-sheet, first in the group). Add Property "Type" dropdown now rendered from the master. | [`2026-05-27-property-types-master.md`](./features/2026-05-27-property-types-master.md) | New org-level `property_types` table (`organization_id` NOT NULL), `properties.property_type_id` relation, deactivate blocked while in use, unique `(org, lower(name))`. |
| 2026-05-27 | Admin · Add Property | **Amenities field: `<select multiple>` → checkbox list** (8 items, `name="amenities"`); removed chip-sync JS. Edit Property is still a placeholder `alert()` (no real form yet). | — (`2026-05-27-property-types-master.md` §related) | App: amenities as a multi-checkbox bound to the org Amenities master; build a real Edit Property form. |
| 2026-05-27 | Admin · Visit Purposes | In-cell deactivate note ("Used by N visits…") moved to the disabled Deactivate button's `title` (canonical wording) — the earlier 6-master sweep had missed it. All 8 masters now consistent. | [`2026-05-27-master-data-ownership-split.md`](./features/2026-05-27-master-data-ownership-split.md) | Same as other masters. |
| 2026-05-27 | Super Admin · Plans / Public | **"Most Popular" is now controllable** — Super Admin `plans.html` sets which active plan is the public highlight (exclusive ★ flag; cleared on deactivate). Signup helper de-hardcoded ("Pick the one marked Most Popular", no plan name); signup tile badge "Popular" → "Most Popular". All read the shared `popular` flag in `assets/plans.js`. | — (extends `plans.js` single source) | App: `plan.is_popular` boolean, exactly one true among active plans; Super Admin PATCH toggles it; home + signup read it. Prototype is session-only (no persistence) — default featured plan stays Standard in `plans.js`. |
| 2026-05-27 | Admin · Property listing | Type-filter tiles → **read-only summary tiles** (Available Units / Total Active Leases / Upcoming Leases, `.is-static`). **Edit Property** per row via a shared add/edit modal; "+ Add Property" reuses it. Assign-Manager field hidden in edit mode (it's the Reassign-PM action on the detail page). | — (`2026-05-27-property-types-master.md` §related) | App: property edit PATCH (no manager field — manager is a separate Reassign action). Summary tiles = aggregate queries. |
| 2026-05-27 | Admin · Property detail | Manager phone unmasked (Admin scope). "Active Leases" section removed (lives on unit detail). Units table + **Current Tenant** column + **Add/Edit Unit** modal (status locked + "Occupied" non-selectable while occupied) + **Retire** (row action, reason required, blocked while occupied — soft status, not delete). **Edit Property** and **+ Create Lease** buttons removed (edit → listing; create → Leases page). | — | App: unit status transitions to/from OCCUPIED are lease-driven; Retire = soft state guarded by no-active-lease + audit reason; PM contact masked for non-Admin roles. |
| 2026-05-27 | Admin · Leases | **NEW `admin/leases.html`** — org-wide lease list (Tenant · Property · Unit · Start · End · Rent · Status · Actions) + filter tiles + property filter + New Lease modal + co-tenant consent panel + pagination. Row View → `unit-detail`. **Leases** sidebar + more-sheet link rolled out to all 17 admin pages (after Properties). | [`2026-05-27-admin-leases-page.md`](./features/2026-05-27-admin-leases-page.md) | Admin Leases = org-scoped query (all properties); PM list is property-scoped. Same lease entity + BL-08/BL-11. |
| 2026-05-27 | Public · Signup | **State → City cascade** — State now a dropdown (NCR states with cities), City cascades from State, both from new single-source `assets/locations.js` (mirrors Super-Admin Cities/States masters). **Pincode** field added (6-digit). | — (`locations.js` single source) | App: org address = state_id + city_id (FK to platform masters) + pincode; signup offers only states/cities the platform serves. |
| 2026-05-27 | All pages | **Global success toast** — `assets/toast.js` + CSS (top-right, auto-dismiss 4s, pause-on-hover, dismissable, `aria-live`, reduced-motion). Loaded on all 64 pages; 38 success `alert()` → `gsToast`, 12 master `announce()` routed, `property-detail` `showSuccess` → toast, signup + contact toast on success. Placeholder "…form opened" / export / PDF stubs left as `alert()`. | [`2026-05-27-global-success-toast.md`](./features/2026-05-27-global-success-toast.md) | App: a `useToast()` hook over a `<Toaster/>` provider fired from mutation `onSuccess`; errors via the `error` variant. |
| 2026-05-27 | Admin · Create Lease | Promoted from modal to a **full page** `admin/create-lease.html` (Unit · Lease terms incl. BL-11 note · Primary tenant · repeatable Co-tenants incl. BL-08 note). Leases-page "+ New Lease" navigates there; flash-toast on return. Removed "+ Create Lease" from unit-detail + property-detail. | [`2026-05-27-admin-leases-page.md`](./features/2026-05-27-admin-leases-page.md) §2a | App: lease create form route; auto-provisions tenant accounts at signing. |
| 2026-05-27 | Super Admin · Org detail | **Reject** action for pending orgs (red button + required-reason modal + new `Rejected` status/badge), alongside Approve. | — | App: org status → `REJECTED` with reason to `audit_log`; applicant notified; no workspace provisioned. |
| 2026-05-27 | Admin · Properties | **Export** button now downloads a real **CSV** (client-side Blob) of the table + success toast (was a placeholder alert). | — | App: server-side CSV honouring filters + audit entry. |
| 2026-05-27 | Admin + PM · Unit detail | Unified + occupancy-aware. Combined Active/Past leases → one **Leases** table w/ Status column; **Current Tenant(s)** card; **Bathrooms** attribute; Monthly Rent moved to the top context card; lease KPI strip; **prototype status simulator** moved to top — when not occupied it hides tenant card, lease KPIs, the active-lease row, and (except *Under Maintenance*) Open Maintenance. **Open Maintenance section removed** from unit detail. Both roles render identical unit data. | — | App: unit status transitions are lease-driven; occupancy gates what renders; `bathrooms` column on `units`. |
| 2026-05-27 | Admin · Property detail | Units table + **Bathrooms** column + **Add/Edit Unit** modal (status locked while occupied) + **Retire** (reason, soft status). Open Maintenance rows → View-detail links. "Active Leases" section removed; manager phone unmasked (Admin scope). | — | App: unit CRUD (Admin only); Retire = soft state guarded by no-active-lease + audit reason. |
| 2026-05-27 | PM · Property/Unit detail | **Parity mirror** of the admin display/lease/preview changes, **without** structural CRUD (PM operations-scoped). | — | Same lease/unit entities, PM property-scoped queries. |
| 2026-05-27 | Admin · Users | **Page overhaul.** Edit + Reset-password forms **merged** into one (single Save; password is an optional field). **Email locked** (disabled). Phones **unmasked** (Admin scope). Admin self-row **removed** (managed from My Profile). **Activate/Deactivate** per row via a confirmation modal (soft status, hides Impersonate while inactive). **Scope** column removed → cols `# · Name · Role · Phone · Status · Actions`. Tenant action reads **Reset** → role-aware modal shows **password-only** for tenants. **Confirm-password** added to both Add-User and Edit/Reset (match validation). Status filter is a **dropdown next to Search** (replaced the Active/Inactive tiles). All below-input helper captions removed. | [`2026-05-27-users-page-overhaul.md`](./features/2026-05-27-users-page-overhaul.md) | `PATCH /users/:id` name+phone only (email/role immutable); password reset audited, no email; user status `active\|inactive` soft + session revoke; phone masked for non-Admin. |
| 2026-05-27 | Admin · Master Data | **NEW Specializations master** (org-level) — `master-data/specializations.html` + single-source `assets/specializations.js` (7 seed, 4 in-use w/ disabled-deactivate tooltip, Painter deactivated); landing card + sidebar/more-sheet sublink (after Amenities) on all 19 admin surfaces. Add-User **Specialization** is now a **multi-checkbox** list (was a single `<select>`) rendered from the master. | [`2026-05-27-specializations-master.md`](./features/2026-05-27-specializations-master.md) | New org-level `specializations` table; maintenance-user ↔ specialization is **many-to-many** (join table); deactivate blocked while in use; unique `(org, lower(name))`. |
| 2026-05-27 | All roles · Listing tables | **`#` serial column** added to every paginated listing table (26 tables across Admin/PM/Maintenance/Super Admin + masters). `assets/paginate.js` stamps a **running, pagination- & filter-aware** number into `[data-pg-serial]` cells (page 2 continues from page 1; restarts per active filter). Server-logs row template (JS-rendered) updated to emit the serial cell. | [`2026-05-27-listing-serial-numbers.md`](./features/2026-05-27-listing-serial-numbers.md) | Pure presentation index = `(page-1)*per_page + i + 1`; rendered from pagination offset, no API/DB change. |
| 2026-05-27 | All listing pages | **`paginate.js` secondary attribute-filter** — new `Paginator.setAttrFilter(tableId, attr, value)` AND-combines a dropdown filter (e.g. a Status `<select>`) with the existing tile filter + search; tile counts respect it. Backward-compatible (other pages unaffected). | — | Maps to a second query param alongside the tile filter in the list endpoint. |
| 2026-05-27 | Tenant role · My Maintenance | Aligned to the Admin/PM maintenance design: kept the **status tiles** (All · Open · In-Progress · Resolved · Closed); added the **combined "Property · Unit" column**; filter bar = **Unit only** (searchable, options show **"Property · Unit"**) · **Priority** · Search — **no Property filter** (a tenant's requests are few units). Raise-request modal gains a **Unit** picker (searchable). Below-input modal captions removed. | [`2026-05-27-searchable-property-unit-dropdowns.md`](./features/2026-05-27-searchable-property-unit-dropdowns.md) | Tenant maintenance list = own requests across their leased unit(s); unit filter scoped to the tenant's units, labelled with property; status workflow `open→in_progress→resolved→closed`. |
| 2026-05-27 | Maintenance role · All Requests | Aligned to the Admin/PM maintenance design: **status filter tiles** (All · Assigned · In-Progress · Resolved · Closed) replace the priority tiles; **combined "Property · Unit" column**; one-row filter bar — **Property → Unit cascade (searchable)** · **Priority** · Search. Sample set expanded to 6 assigned requests so the status filter is meaningful. Scoped to the staff's own assigned requests (no Assignee filter — all are theirs). | [`2026-05-27-searchable-property-unit-dropdowns.md`](./features/2026-05-27-searchable-property-unit-dropdowns.md) | Maintenance list = requests assigned to the signed-in staff; status workflow `assigned → in_progress → resolved → closed`; property/unit/priority filter params. |
| 2026-05-27 | All roles · Property/Unit pickers | **Searchable dropdowns (rule #18).** New shared `assets/searchable-select.js` (+ `.gs-combobox*` CSS) progressively enhances any `<select data-searchable>` into an accessible type-to-filter combobox (native select stays the form value + validation source, re-dispatches `change`). Applied to **17** property/unit pickers across 9 pages (create-lease, leases filter, maintenance filters + raise + reassign, property-detail, rent lease-picker, pm leases, pm rent-collection, **+ a new Property field on the PM raise-request modal**). Maintenance Property→Unit cascade calls `SearchableSelect.refresh()`. Other selects (status/role/category/plan/type/per-page) stay native. | [`2026-05-27-searchable-property-unit-dropdowns.md`](./features/2026-05-27-searchable-property-unit-dropdowns.md) | App: property/unit pickers use a searchable Combobox bound to the same options (rule #18); non-property/unit selects stay plain. |
| 2026-05-27 | Admin + PM · Maintenance | **Listing reworked into one shared design (admin built first, then cloned to PM).** Single row of **status filter tiles**: All · Open · In-Progress · Resolved · **Closed** (Closed = resolved then closed out by tenant/PM/admin). Removed the redundant KPI stat-grid (one tile row only). **Tenant column removed** (any role can raise a request → it was often blank). Unit + Property merged into **one "Property · Unit" column** (property name + unit beneath — the standing convention for unit+property in any listing table). One-row filter bar (`md:grid-cols-5`): **Property → Unit cascade** (`setAttrFilter` on `data-property`/`data-unit`; Unit lists only units with ≥1 request for the chosen property) · **Priority** (`data-priority`) · **Assignee** (`data-assignee`, incl. Unassigned) · Search. Row action is **View** only (Reassign / Reopen / Close removed from the listing — they live on the detail page). **Both roles get "+ Raise Request"** (anyone can raise; Admin's modal also picks Property). Same 12-request dataset (PM scoped to its properties; Admin sees all). | — | Maintenance list = role-scoped query (PM: own properties; Admin: all). Status workflow `open → in_progress → resolved → closed`. Status/property/unit/priority/assignee are server filter params; unit options limited to units with requests. Raise-request available to PM + Admin; assignment/close/reopen on the detail view. |
| 2026-05-27 | PM · Tenants | **Reduced to a Users-style people directory.** Table is now `# · Tenant · Property·Unit · Phone · Status` (the unit cell shows the **property name** above the unit) — **one row per person** (co-tenants expanded into their own rows). Dropped lease/rent/deposit columns + the Sort control; removed **"+ Add Tenant"** (tenants auto-create at lease signing). Status = account **Active/Inactive** with a Status **dropdown** (`setAttrFilter`) + Search + serial `#`. **`pm/tenant-detail.html` deleted** — no detail page / no View action (contracts live under Leases; the directory is contact-only). PM sidebar reordered: **Leases now before Tenants**. | [`2026-05-27-pm-tenants-directory.md`](./features/2026-05-27-pm-tenants-directory.md) | PM Tenants = people directory scoped to the PM's properties (read-only + contact); distinct from the lease list. No tenant CRUD from PM. Phone masked for PM scope. |
| 2026-05-27 | Tenant · Visitors | **Visitor Code (`VIS-XXXX`) added.** Pre-approve form gains a **Unit** searchable picker (property · unit label). On submit, a 4-char alphanumeric **Visitor Code** is generated and shown to the tenant via toast + displayed in the table (tenant shares it with their visitor). Table gains `Visitor Code` + `Property · Unit` columns. | — | `visitors.visitor_code` = unique 4-char token generated at pre-approval; shown to tenant only; validated at PM/Admin check-in. |
| 2026-05-27 | PM · Visitors | **Check-in code validation + lifecycle fixes.** PM check-in modal now requires the PM to **enter the code the visitor presents** — validated against the stored code (wrong code blocked). `Property · Unit` combined cell (with tenant sub-line). Denied tile added. Fixed off-by-1 `confirmAction` cell-index bugs. Footer caption removed. | — | Check-in = enter code → validate against `visitor.code` → timestamp check-in; mismatch = 422. |
| 2026-05-27 | Admin · Visitors | **NEW `admin/visitors.html`** — org-wide visitor log (all 6 status tiles, Property→Unit cascade filters, 11 rows across 4 properties, full Approve/Deny/Check-in/Check-out). Same check-in code-validation flow as PM. Visitors added to all **21 admin sidebar + more-sheet** nav surfaces (after Maintenance). | — | Admin visitor query = org-scoped (all properties); PM = property-scoped. Same `visitors` entity + code validation. |
| 2026-05-27 | All · Maintenance raise form | **Title field** added before Description on all 3 raise-request modals (Admin, PM, Tenant). **Priority** standardised to **radio list** on all roles (Tenant had a `<select>`). **Admin + PM**: Property picker now blank-first + required; Unit starts disabled and cascades from the selected property (`REQ_UNITS` map). | — | `maintenance_requests.title` NOT NULL; priority = enum radio; unit_id required at creation. |
| 2026-05-27 | All · searchable-select bug | **Horizontal scroll fix** — native `<select data-searchable>` was left as a sibling of the combobox wrapper (wrong positioned ancestor) and inherited `width: 100%` from a higher-specificity `.input` rule, causing page-level scroll on all pages with searchable dropdowns. Fixed: select moved **inside** the wrapper; CSS overrides with `\!important`. | — | No API/DB impact. |
| 2026-05-28 | All roles · Maintenance detail | **Status simulator added to all 4 role detail pages** + role-appropriate content throughout. Admin: context card fixed (Monthly Rent removed → Property Manager; Lease → Tenant); Assignment History table removed (timeline covers it); Admin can now add notes to the timeline. PM: context card updated (+Assignee col); status-aware actions (Open/Assigned/In-Progress/Resolved/Closed) without cross-property reassign; Attachments + caption lines removed. Maintenance staff: only Mark In-Progress + Mark Resolved available (Acknowledge/Pause/Resume removed); 3-button simulator (Assigned · In-Progress · Resolved). Tenant: context card updated (+Assignee/PM cols); Close + "Still an issue" buttons enabled only on Resolved; captions removed; 5-button simulator with tenant-friendly labels. All pages: Emergency checkbox toggles priority badge between Emergency (red) and High (orange). | — | Status workflow `open → assigned → in_progress → resolved → closed`; each role's actions enforce BL-21 (only tenant/PM/Admin can close). Maintenance staff transitions: in_progress + resolved only. |
| 2026-05-28 | Admin · Create Lease | Wizard wrapped in a single outer `card` (no more nested per-step cards / no more grey gap around the surface). Unit-status badges use semantic colours (Available green, Occupied/Listed blue, Maintenance amber, Retired grey) via a new `unitStatusBadgeHtml`. Step 4 rebuilt with three modes: a real searchable tenant dropdown (`+ Create new tenant` as a sticky fallback) → existing-tenant compact card showing **name + email only** (phone hidden as PII) → new-tenant input form. Validation rejects rows still in `search` mode. | — | App: tenant directory `GET /tenants?q=` for the dropdown; existing-tenant rows submit `tenant_id`; new-tenant rows submit `name/phone/email` (account provisioned at lease sign per BL-04). |
| 2026-05-28 | Public · Org sign-up | "Type of Business" is now a **multi-select chip group** (`renderBusinessTypeCheckboxes`) spanning the full row — an org can legitimately operate in multiple categories (PG + Co-living + Society). Card widened (max-width 1100 / 1200 at ≥1440px); two-column grid kicks in at 768px; shell gutter trimmed to 16px. **State + City selects are now searchable** (`data-searchable`); cascade calls `SearchableSelect.refresh('city')`. | — | App: org ↔ business-type is many-to-many; `validateBizTypes` requires ≥1. |
| 2026-05-28 | Admin · Delegations | "Created by" column removed from Active/Upcoming/Expired tables (Revoked keeps "Revoked by"). The **New Delegation modal moved to its own page** `delegation-new.html` (a delegation can grant 10+ tasks — needs page-scale real estate). Listing's "+ New Delegation" is now a link; old modal markup + handlers + modal-only CSS deleted; the listing reads `?created=1&target=<tab>` on return and toasts. | — | No API change — same `POST /delegations` payload from the new page. |
| 2026-05-28 | Super Admin · Plans + Org detail | Per CLAUDE.md rule #19: Plan **slug input removed** from the create/edit form; primary identifier is numeric auto-increment DB PK; slug retained only as a `VARCHAR UNIQUE` secondary column. URLs and Change-Plan radios use numeric ids. **Reactivate button is always enabled** on retired plans (was disabled). Deactivate is the only one gated (≥3 active plans must remain). | — | App: `plans.id INTEGER PK AUTOINCREMENT`; `plans.slug VARCHAR UNIQUE`; `?id=<n>` everywhere. |
| 2026-05-29 | Super Admin · Approve modal | Promoted from a 3-line confirmation to a 720 px review surface. Sections: (A) Organization details table (name · business type · units · location · plan · submitted on); (B) Primary Contact card (read-only by default, "Change contact" toggle enables inputs) with **email-collision detection** — red banner + Approve-button gate when the applicant's email is already in use; (C) **Billing cycle** — anchor day picker (1–28, default = today, "today (recommended)" marker) + live first-invoice preview that pro-rates when anchor ≠ approval day. Toast on submit names the chosen anchor. | — | App: `organizations.billing_anchor_day SMALLINT CHECK BETWEEN 1 AND 28 NOT NULL`. Approval also auto-creates the org's first invoice via the anchor-day cron logic in NR-13. |
| 2026-05-29 | All billing surfaces · Anchor-day billing model | Every org now carries its own `billing_anchor_day`. Calendar-month invoices removed. **25 mock invoices** on `super-admin/invoices.html` re-anchored across 8 orgs with distinct anchor days (5/8/12/15/18/21/25/28) so the listing demonstrates billing-load spread across the month. Both invoice-detail pages, the admin billing/organization page, and the SRS NR-13 + planning doc §2.2.2 are aligned with the new model. | — | App: cron runs daily at 00:00 IST and issues invoices for orgs whose `billing_anchor_day = today`. Period = `[anchor of this month, anchor − 1 of next month]`. Approval-time invoice is a full cycle if anchor = approval day, pro-rated otherwise. |
| 2026-05-29 | Super Admin · Mark Paid modal | Free-text "Payment reference" replaced with a **Payment Method dropdown** (pulled from new canonical `assets/payment-methods.js` — UPI/NEFT/Cash/Cheque/IMPS) + a Payment Reference field whose **label and placeholder swap based on the selected method** (UPI → "UPI transaction ID"; Cheque → "Cheque number"; etc.). Both required. Invoice header card gained a "Payment Method" row that appears when status = paid. Status Timeline entry includes the method. | — | App: `invoices.payment_method_id INTEGER` FK-equivalent (via Prisma `@relation`) to the Payment Methods master. `payment_reference` stays free-text. |
| 2026-05-29 | Super Admin · Invoices listing | "+ Generate Invoice" button removed (was a disabled v2 placeholder). Manual invoice generation is a recovery operation that doesn't belong as a permanent UI button; if needed later, it ships as a v2 admin tool. | — | No API impact. |
| 2026-05-29 | Super Admin · Org detail Subscription card | Replaced the redundant "Utilization" bar (it duplicated the "Active users · 6 of 20" row above it) with a **Features available** chip list rendered from `GHARSETU_PLANS[].features` + `GHARSETU_CORE_FEATURES`. Saffron-green-tinted pills; no ✓ ticks. Added a **Billing anchor day** row. Cleaned up a future-dated row in Subscription Plan History and a spurious "Plan changed Basic → Standard" audit row. | — | Frontend-only rendering changes; plan features are already plan-derived data. |
| 2026-05-29 | Admin · Billing → Organization page | **`admin/billing.html` deleted**; new `admin/organization.html` mirrors the Super Admin's org-detail layout but read-only. Includes status strip, Organization Details + Subscription cards (Plan, Plan price, Billing anchor day with next-invoice date, Active-user cap, Active users, Features available chips), Subscription Plan History, and the existing Invoices table. **Sidebar + more-sheet "Billing" → "Organization"** across 20 admin top-level pages + 5 master-data sub-pages (icon also swapped from credit-card to the building glyph). | — | Admin's read-only own-org view. Plan-change / deactivate / impersonate remain Super Admin-only. |
| 2026-05-29 | Admin · Invoice detail | New `admin/invoice-detail.html` — read-only own-side invoice view. Previously the admin reused `super-admin/invoice-detail.html?admin=1`, leaking the `/super-admin/` URL into the Admin's address bar. Now lives entirely under `/admin/`. Has no Mark Paid or Cancel buttons (Super Admin-only mutations). Simulator limited to Issued / Paid / Cancelled (no Draft — admins never see drafts). "← Back to Organization" footer link. | — | Same `invoices` model; role-gated by URL prefix in the live build. |
| 2026-05-29 | Admin · New Delegation form | Expanded from 10 tasks to **30 admin tasks across 8 capability groups** (Property & Inventory 8 · Leases 4 · Rent & Payments 2 · Maintenance 4 · Visitors 2 · Users & Team 4 · Master Data 5 · Audit & Reports 1). Each group is a card with title + "X / N" count badge + "Select all" toggle (with indeterminate state). Sticky chips-preview box shows total selected. Existing seed labels kept verbatim so listing rows still render. | — | No new tasks beyond what an Admin can already perform; UI surfaces the full delegatable set. |
| 2026-05-29 | Marketing + Sign-up · Plan tiles | Sign-up plan tile redesigned: floating "Most Popular" badge above the card, big centred price (30 px Poppins), two grey-tinted cap KPI chips, divider, 2-column feature list (single column < 640 px). URL `?plan=<id>` from the marketing card now auto-checks the matching radio so "Get started" lands pre-selected. Marketing pricing cards + Super Admin plan cards + sign-up tiles all use the new `gsCombinedFeatureList` helper — flat unified Core + Optional list, no Core/Optional labels (those only matter in the Plan editor modal where Core is read-only and Optional has checkboxes). | — | Frontend-only. Plans data unchanged. |
| 2026-05-29 | Plans data | **Priority Support** feature removed from the catalogue (was a vague marketing flag with no defined deliverable). **Task Delegation** removed from Standard plan (now Premium-only). New **`GHARSETU_CORE_FEATURES`** constant (Properties · Units · Rooms · Tenants · Leases · Users) — always included on every plan, never toggleable. Rooms is Core (not gated by Per-Room Leasing) so inventory data survives any plan downgrade; Per-Room Leasing now strictly gates the *lease creation flow*, not the rooms entity. | — | App: feature catalogue + plan→features many-to-many; core entities are not in the feature_flags table. |
| 2026-05-29 | All cascading dropdowns · Bug fix | `SearchableSelect.refresh()` was silently no-op'ing because it looked for the wrap via `sel.previousElementSibling`, but post-enhancement the native `<select>` lives **inside** the wrap. Switched to `sel.parentNode`. Other call sites coincidentally still worked (their selects never toggle `disabled`); the signup City picker was the only one broken because it toggles disabled on state change. | — | No API impact. |
| 2026-05-29 | Solution Overview docx | **Restructured per client feedback.** Added Introduction. Renamed "Fixes in Existing Modules" → "Incomplete Features in Current System" + expanded to 8 modules (added Settings + Master Data rows) + closing paragraph pointing to a separate Bugs & Gaps Excel sheet. Merged "New Roles" + "New Features" into a single "New Features as Per Requirement" major section with 5 sub-sections (Per-Room Leasing, Admin Impersonation, Task Delegation, Visitor Management, Organization Management). Removed: Subscription Plans table, Admin Impersonation Scope table, Admin Task Delegation paragraph, Settings — Defaults and Tunable Ranges table, Assumptions section. Cover date updated to 29 May 2026. | — | No app-port impact (documentation only). |
| 2026-05-29 | Admin · Dashboard | Removed the standalone **Emergency · Unit 7C (Green Valley, Dwarka)** water-leakage alert banner from the Admin dashboard. The Emergency count badge and the matching maintenance-table row are unchanged. | — | Frontend-only. |
| 2026-05-29 | Maintenance priority labels · system-wide | **Enforced the 4 canonical priority levels everywhere: Low · Medium · High · Emergency — no other labels.** The stray **"Normal"** priority badge (used in Admin, PM and Maintenance dashboards, `badge-prepaid`) was renamed to **"Medium"** to match the SRS (§ request fields: low/medium/high/emergency) and the existing dropdowns/filters. No `data-priority` values used "normal", so filtering is unaffected. | — | Aligns prototype with SRS; live `maintenance_requests.priority` smallint enum already = {low,medium,high,emergency}. |
| 2026-05-29 | Organization detail (Admin + Super Admin) | **Type of business now shows multiple, comma-separated** (was a single value) — matching the signup form, which is already a multi-select checkbox group ("select all that apply"). Updated `super-admin/organization-detail.html` (details card + approve-review modal table) and `admin/organization.html` details card. Sample data: Saffron Stays → "Co-living, PG / Hostel"; Triline → "Property Management Firm, Housing Society". **Also added the Primary Contact to the Organization Details card** (Primary contact name · Contact email · Contact mobile) on both pages — previously the contact only appeared in the top status strip (name + email, no phone). SRS §4 organization-record line updated to "one or more business types (multi-select … many-to-many, displayed comma-separated)" + "primary contact person + email + mobile". | — | App: `organizations` ⇄ `business_types` is many-to-many; org detail serializes the list comma-separated. Primary contact (name/email/mobile) is the first Admin user's profile. |
| 2026-05-29 | Login + Forgot password · email-only auth | **Login is by email only — mobile is not a login identifier.** Removed all "Email or Phone" wording: `login.html` field label "Email or Phone" → "Email" + placeholder `raj@gharsetu.in or 98xxxxxxxx` → `raj@gharsetu.in`. `forgot-password.html` label, intro copy, error-hint bullet, "Try a different email" button all de-phoned; `validateIdentifier()` rewritten to accept email only (dropped the 10-digit Indian-mobile branch). Profile/contact/user-creation **phone fields kept** (phone is still profile data, just not auth). SRS §3 Login row updated to "Email + password sign-in (email only)". | — | App: auth identifier = email; `users.phone` stays a profile column, never an auth lookup. Matches BL-04 (email becomes the login credential at lease signing). |
| 2026-05-29 | Login · Prototype Shortcuts strip | Centered the "Prototype Shortcuts — Preview as Role" strip under the auth card — was `max-width:520px` with no horizontal margins (sat left / overhung the card). Now `max-width:480px; margin:32px auto 0` so it matches the login card width and centers beneath it. | — | Prototype-only convenience strip; not part of the product. |
| 2026-05-29 | Super Admin · Contact Inbox · Bug fix | "Mark as read / replied" wrote the new status badge into **`td:nth-child(5)` (Subject)** instead of the Status column, clobbering the subject text and never updating the real Status cell. Table columns are #, When, Name, Email, Subject, **Status (6)**, Action — corrected the selector to `td:nth-child(6)`. | — | No API impact (prototype JS index bug). |
| 2026-05-29 | Admin · Unit Detail · Bug fix | The "Preview status" simulator left the **"+ Create Lease for this Unit"** button (and the per-room "+ Create Lease" buttons) visible even when status = **Retired** or **Under Maintenance** — but neither accepts new leases. `setUnitSimStatus()` now hides the unit-level button (new id `create-lease-unit-btn`) and all room create-lease links (`a[href*="create-lease.html"][href*="roomId"]`) when status is retired **or** under maintenance; they return for Available / Listed / Occupied. | — | Status gates the create-lease entry points: no new leases on a retired or under-maintenance unit. |
| 2026-05-29 | Admin · Unit Detail | Leases table gains a **Type** column (Unit-wise / Room-wise · Room X) showing what kind of lease each is. The **Leases section moved to after "Rooms in this unit"** (order is now Current Tenant → Rooms → Leases). JS-referenced row ids (`lease-active-row`, `lease-upcoming-row`) preserved, so the status-simulator gating still works. | — | App: `leases.lease_type` (unit-wise / room-wise) + optional `room_id`; surfaced as the Type column. |
| 2026-05-29 | Admin · Lease Detail + Create Lease · Renew Lease feature | **"Renew Lease" now opens the Create Lease wizard in renew mode** (`create-lease.html?renew=<leaseId>`) instead of a "coming soon" toast. In renew mode: title → **"Renew Lease"**, wizard lands on the **Tenants step (4)**, property/unit/room are **locked** (Back is clamped to step 4 — `showStep`/`wizardBack` honour `wizardState.renewMode`; minStep=4), tenants are **pre-filled** from the lease (existing-tenant cards, still editable via Change/Add), and **monthly rent + security deposit pre-fill from the old lease** (not the unit default — `prepareStep5` uses `renewRent`/`renewDeposit`). Step-5 CTA reads "Renew Lease". Lease-detail renew control changed from `<button>` to `<a href>` (status-show toggle still works). Seed: `RENEWALS['2103']` = unit-wise, unit 103, ₹18,000 / ₹36,000, tenants Rohan + Priya. | — | App: renew = create a new (upcoming) lease seeded from the prior lease's unit/room + tenants + rent/deposit; property/unit/room immutable in the renew flow. |
| 2026-05-29 | Admin · Lease Detail | Removed the **"View profile"** links from the tenant cards (primary + co-tenant) in the Tenants block. These were the only "View profile" buttons anywhere under `admin/` — none remain. | — | Frontend-only. |
| 2026-05-29 | Admin · Lease Detail · Early Termination flow | **Removed the top "Early Terminate" button**; replaced with a new **Early Termination section** at the end (after Rent Change Schedule). Section explains every tenant must raise a termination request (BL-08/BL-09 — explicit per-co-tenant consent, no auto-approval), shows a per-tenant table (Tenant · Role · Request status · Requested on · Action) where each tenant can "Raise request" (status Pending→Requested + timestamp), a live "N of M tenants have requested" progress line, and an **Early Terminate button that stays disabled until all tenants have requested**. When enabled, it opens a modal requiring a **reason** (min 5 chars); confirming sets the lease status to Terminated and toasts. Section is `data-status-show="upcoming,active,terminated"`: for **terminated** it renders as a **read-only record** (all tenants Requested, no action buttons, "terminated after all tenants requested" note) via `applyTermViewForStatus()` wired into the status simulator; for active/upcoming it's the interactive consent flow (resets to Pending). `raiseTermRequest` hides (not removes) the button so previews reset cleanly. | — | App: termination requires every co-tenant's explicit request (BL-08/BL-09); Admin records a reason; status → terminated; audit row written. |
| 2026-05-29 | PM · Property & Lease parity (assigned-scoped) | **Cloned the Admin property/lease feature set to the Property Manager role, scoped to assigned properties only.** New **`pm/create-lease.html`** (clone of admin wizard incl. renew mode; PM chrome; `PROPERTIES` scoped to the 3 assigned — Green Valley, Sai Heights, Mayur Vihar — Rohini Greens/property-4 removed from active data). **`pm/unit-detail.html`** brought to parity (Rooms section + Leases "Type" column after Rooms; "+ Create Lease for this Unit" + per-room buttons → `create-lease.html`; gated hidden when Retired/Under-Maintenance). **`pm/lease-detail.html`** brought to parity (Renew → `create-lease.html?renew=2103`; per-co-tenant-consent **Early Termination** section + Terminated read-only record view; no "View profile"). **`pm/leases.html`** "+ New Lease" + row "Renew" now open the real wizard (stub modal + `alert()` removed); banner no longer mentions a "Renewed" status. PM nav/chrome preserved on all (sidebar/tabbar/more-sheet/account swapped from PM pages; Properties active on unit-detail). `pm/properties.html` + `pm/property-detail.html` already assigned-scoped with no admin-only actions — verified, left intact (kept their PM past-tenure sections). | — | App: PM is org-scoped to assigned properties (BL-19); creates tenants & leases, renews, terminates-with-consent on assigned properties only; never cross-property/cross-org. |
| 2026-05-29 | PM · Leases listing — adopt admin layout | `pm/leases.html` re-cloned from `admin/leases.html` (the old PM list was outdated): filter tiles (All / Upcoming / Active / Ended / Terminated), searchable Property filter, columns # · Lease # · Lease Type · Property·Unit · Tenant(s) · Rent · Status · Action. Chrome swapped to PM (Leases active in sidebar + more-sheet); **Rohini Greens removed from the property filter**; all 8 rows are assigned properties (Green Valley / Sai Heights / Mayur Vihar); "+ New Lease" → `create-lease.html`; rows → `lease-detail.html?id=…`. Canonical statuses throughout. | — | PM leases list = assigned-property leases only, admin-grade filtering. |
| 2026-05-29 | Lease status labels · system-wide | **Standardized lease statuses to the fixed set Active / Upcoming / Ended / Terminated** (lease-detail is canonical). No "Renewed" status (pm/leases badge + banner fixed → Ended); lease-detail status-simulator button "Expired" → "Ended" (admin + pm) so the control matches the badge. Left untouched: maintenance "Closed", delegation "Expired", unit "Listed" (not lease statuses). | — | App: `leases.status` smallint enum = {upcoming, active, ended, terminated}; no renewed state. |
| 2026-06-01 | Shared infra · pagination + clickable cards | **`assets/paginate.js`**: added `Paginator.setPredicate(tableId, fn)` — a reusable custom-row predicate AND-combined with search/tile/attr filters (powers all date-range filters). **`assets/styles.css`**: added `a.kpi` styling (hover lift, focus ring, reduced-motion) so any KPI/metric card can become a deep-link. | — | App: list filters compose a predicate set; metric cards link to filtered lists. |
| 2026-06-01 | Super Admin · Dashboard / Organizations / Contact Inbox | Dashboard KPI cards now clickable → filtered pages (Pending → `organizations.html?filter=pending`, etc.). Organizations: **Contact column** + **Created-date range filter** + contact added to search; rows carry `data-contact`/`data-created`. Contact Inbox: **Received-date range filter** via `setPredicate` (rows carry `data-date`). | — | — |
| 2026-06-01 | Admin · Dashboard / Master Data | All 8 dashboard KPI cards clickable → filtered pages/sections. **"Specializations" → "Maintenance Specializations"** across 25 admin files (nav, titles, headings; file/route unchanged). | — | — |
| 2026-06-01 | Admin · Leases / Properties | Leases: wired the previously-dead **Property filter** + new **Lease-Type filter** (rows carry `data-lease-type`). Properties: **Total Rooms column**, **Property-Type + Room-Status filters** (wired), **Manager filter** wired, summary **tiles now clickable** (Available Units → create-lease; Active/Upcoming leases → filtered leases). Rows carry `data-rooms`/`data-has-rooms`/`data-manager`. | — | App: `properties.total_rooms`, room-status derive. |
| 2026-06-01 | Admin · Delegations + NEW Delegation Activity | Delegations: added an **All** status tab (shows every panel). **New `admin/delegation-activity.html`** — delegation-action audit (When · Delegate · Role · Delegation window · Action · Target) with search + role filter + date-range (`setPredicate`) + pagination; linked from Delegations header ("Activity Log"). | — | App: delegation_activity audit view. |
| 2026-06-01 | Admin · Maintenance / Rent | Maintenance: **Room cascade filter** (property→unit→room via `MAINT_ROOMS`) + **Room field in the Raise Request form**. Rent: **Room column** + **5 filters (Tenant search · Property · Unit · Room · PM)** wired + **Room field in Record Payment**. Rows carry `data-property`/`data-unit`/`data-room`/`data-pm`. | — | App: maintenance & payment room scope. |
| 2026-06-01 | Admin · Settings / Organization | Settings: **Field + Date-range filter** over the Recent-changes table (rows carry `data-field`/`data-date`). Organization: **Status + Plan + Issued-date filters** over Invoices (rows carry `data-status`/`data-plan`/`data-date`; one invoice set to Issued so Status filter is meaningful). | — | — |
| 2026-06-01 | PM · Dashboard / Properties / Leases | Dashboard: all 8 KPI cards clickable → filtered pages (assigned-scoped). Properties: **Rooms column** + **Room-status filter** (rows carry `data-rooms`/`data-has-rooms`). Leases: wired the **Property filter** + added **Lease-Type filter** (the clone had inherited the dead filter). | — | All assigned-scoped. |
| 2026-06-01 | Super Admin · Organizations · Pagination consistency fix | **Bug:** the page had a bespoke pagination footer (`flex justify-between` → info left, ‹/› right, no page numbers / per-page selector) that looked different from every other listing (which uses the shared centered `.pagination` component). **Fix:** migrated `organizations.html` fully to the shared **Paginator** — status pills → `data-tile-for`/`data-tile-filter` tiles, Plan → `setAttrFilter`, search → `data-pg-search`, Created-date → `setPredicate`, and the standard `.pagination` markup (`data-paginate-for="org-table"`). Removed all bespoke filter JS; added a small shim so the dashboard deep-links (`?filter=pending`, `?plan=…`) still apply on load. Now one constant pagination design across all pages, and the page's pagination/tile-counts are functional. | — | One pagination component everywhere. |
| 2026-06-01 | NEW ROLE · Security Guard + gate-initiated visitor approvals | **6th role `SECURITY_GUARD=5`** (org-scoped, assigned properties, Visitor-Management-only, email login). **New `prototype/security/`**: `dashboard.html` (gate KPIs, clickable), `visitors.html` (gate console — "Log Gate Arrival" modal routes an approval request to the tenant by lease type; common-area = guard approves directly, no Visitor Code; approve/deny/check-in/out; `awaiting` tile; filters + pagination), `profile.html`; guard-only nav (Dashboard · Visitors · Profile). **`tenant/visitors.html`**: "Pending approvals" section (any one co-tenant approves/denies gate requests). **`admin/users.html`**: Security Guard role option + assigned-properties checkboxes. **`admin/visitors.html` + `pm/visitors.html`**: "Awaiting tenant" tile + gate-initiated row (`data-status="awaiting"`, "Gate" badge). **`login.html`**: Security Guard preview shortcut (grid 5→6). SRS updated: roles 5→6, matrix (Guard row + amended Tenant), enum `SECURITY_GUARD=5`, Module 8 gate flow + `awaiting_tenant_approval` status, page map. Plan: `docs/planning/features/2026-06-01-security-guard-role.md`. | — | App: `users.role` += SECURITY_GUARD; `security_guard_properties` join; `visitor_requests` gains initiated_by/target_unit/target_room/lease_id/approved_by_tenant + nullable code; new statuses. In-app only (no SMS/WhatsApp/email). |
| 2026-06-01 | Visitors · Type column + tenant pending-approvals fix | **(1)** Added a **Type** column (**Pre-approved** vs **Gate**) to every role's visitor table (admin/pm/security/tenant) so it's clear which flow each visit came from; removed the redundant inline "Gate" badge. **(2)** Tenant "Pending approvals" section now only renders when a gate request is actually awaiting — the heading + "a guard has logged a visitor…" description hide once the last request is resolved (`refreshPendingApprovals()`), fixing the contradictory "No pending gate approvals" state. **(3)** Fixed broken HTML comments (`<\!--` → `<!--`) on `security/dashboard.html` + `admin/delegation-activity.html` that were rendering as visible text (zsh mangled `!` in a heredoc). | — | App: `visitor_requests.initiated_by_role` surfaces as the Type column. |
| 2026-06-01 | Visitors · Type filter + code search + Property/Unit filter (all roles) | Across **admin / pm / security / tenant** visitor tables: added a **Visitor type** filter (Pre-approved / Gate, wired to `data-type`); **search now matches the hidden visitor code** — shared `trMatchesSearch` extended to also scan a `data-search` attr (so the code stays hidden per SRS but is findable), rows carry `data-search="VIS-…"`, placeholders updated. Added **Property + Unit** filters on admin (existing cascade) and **pm/security** (new selects wired to `data-property`/`data-unit`; rows parsed to add those attrs). Tenant is single-unit so it gets Type + code search only. | — | App: visitor list filters = type + property + unit + free-text incl. code. |
| 2026-06-01 | Security Guard · Users page + tenant dashboard surface | Follow-up completing the role's UI presence: `admin/users.html` gains a **Security Guards filter tile** (`data-role="security-guard"`) + **two guard rows** (assigned-property scope shown next to name) with Edit · **Impersonate → security/dashboard.html** · Deactivate. `tenant/dashboard.html` gains a **"visitor waiting at the gate" alert** linking to `visitors.html#pending-approvals`. Verified login's redesigned role picker already lists Security Guard. | [`2026-06-01-security-guard-role.md`](./features/2026-06-01-security-guard-role.md) | App: guard users listed/filtered by role; impersonation target = security dashboard; tenant in-app approval surfaced on dashboard. |
| 2026-06-01 | Admin · Rent Collection — Record Payment full page | **Converted the Record Payment popup into a full page** `admin/record-payment.html` (replaces the modal). Two-column layout: payment form (lease searchable-select · room · amount · date · method with method-aware reference label · notes) + a live **Lease summary** card (tenant · property·unit · PM · monthly rent · outstanding · status) that populates **on lease select**, plus a full-width **Previous payments** table showing that lease's history (date · period · amount · method · reference · status). Amount auto-fills from outstanding. Submit → inline success state ("Record another" / back). `rent.html`: "+ Record Payment" + all 20 row "Record" links now navigate to the page (modal removed); `?lease=<id>` preselects. | — | App: record-payment is its own route; lease select fetches summary + payment history; payments append-only (record a new payment to correct). |
| 2026-06-01 | Admin · Record Payment — late fee + month allocation | Added an auto **Payment allocation** table below Notes on `admin/record-payment.html`. Each lease now carries a **due-month schedule** (`dueMonths` oldest-first, with an overdue flag) + `nextDue`. **Late fee auto-calculates** at **2% of monthly rent per overdue month** (Settings → Late-Fee Rate). On lease select / amount change, the entered amount is **allocated oldest-first** — each overdue month shows Rent + Late fee + Total due + Amount applied + Status (Covered / Partial / Unpaid); **surplus rolls into future months as Advance**. Lease summary now shows Late-fee/month + Outstanding **incl. late fee** (amount auto-fills to it). Summary line: "Fully covers N months · ₹X advance · ₹Y still outstanding". | — | App: late fee derived from Settings rate × overdue months; payment allocation = oldest-due-first incl. late fee, surplus = advance/prepaid. |
| 2026-05-30 | Admin · Create Lease | **Design modernization pass.** (1) Progress indicator: 32 px step circles, saffron glow ring on active step, `aria-current="step"`, `panelFadeIn` animation on step panels (180 ms, respects `prefers-reduced-motion`). (2) All 5 steps get Poppins 700 `step-heading` + 14 px `step-subheading`; Step 3 heading updates for room-wise mode. (3) Property/Unit/Room cards: 44 px colored band (navy/blue tones, hashed from id) with initials + saffron checkmark badge when selected; hover lift `translateY(-3px) + box-shadow`. (4) Lease-type cards: `::after` checkmark circle, saffron tint + glow on `is-selected`. (5) Conflict error promoted from a `.field-error` line to a full amber alert banner (`cl-conflict-banner`) with warning SVG icon, bold title, detail text, resolution hint. (6) Live lease summary right rail (268 px, sticky) shows from Step 2 onward — type pill, property, unit, room, primary tenant, dates, rent, deposit; hidden on mobile. (7) Two-column `wizard-shell` layout wraps body + summary rail; collapses on mobile. (8) Bottom bar gains "Step N of 5" counter. (9) Step 5 context panels (Lease history + Tenant lease history) are now accordion panels with chevron-rotate toggle; Lease history open by default. | — | App-port: `updateSummary()` maps to a React state object built from wizard form state; conflict banner maps to a styled `<Alert>` component. Context-panel accordion maps to a `<Disclosure>` component. |
| 2026-05-30 | Admin · Create Lease | **Step 5 restructure — richer form + selected-tenants panel + 6:6 history grid.** (1) Form reorganized into 4 labelled sub-sections (Lease term / Rent & charges / Rules / Notes) using `.step5-section-label` (Poppins 700 / 11 px / uppercase / 2 px saffron bottom border). (2) Three new fields: Lock-in period (`cl-lockin`, default 6 months), Maintenance charges (`cl-maintenance`, default ₹0), Rent escalation (`cl-escalation`, default 5%/yr). Special terms textarea (`cl-notes`, optional, max 500 chars). (3) Step 5 right column replaced: accordion context panels out, **Selected tenants** panel in — shows avatar + name + Primary chip + email/phone + lease-history badge per tenant; updates live as new-tenant details are typed. (4) Two accordion context panels (lease history + tenant history) relocated **below** the form in a new `step5-history-grid` (true 50/50, collapses to single column at <1024 px); rendered as flat always-visible cards (no accordion). `panel-unit-history-body` + `panel-tenant-history-body` ids preserved. (5) Summary rail updated with Maintenance / Escalation / Lock-in rows. | — | App-port: lock-in, maintenance, escalation, notes map to new `leases` columns (`lockin_months SMALLINT`, `maintenance_paise BIGINT`, `escalation_pct NUMERIC(5,2)`, `notes TEXT`). Selected-tenants panel is a pure read-from-DOM render — no API call needed. History panels remain read-only from existing lease history endpoint. |
| 2026-05-30 | Admin · Create Lease | **Step 5 trim — Lock-in / Rent escalation / Notes / Late fee / Notice period / Rent due day all removed** (user flagged they were over-scope vs the original brief). Form now carries exactly 5 fields: Start · End · Monthly rent · Maintenance · Security deposit. **Reasons:** start/end define the term so lock-in is termination-time-only; escalation column would be no-op (no cron); free-text notes were UI theatre. Late fee + Notice period are platform-level Settings; rent due day derives from start date; grace period is the existing global Setting applied at collection time. **Tenant Lease History rows enriched** — property → unit (→ room) + monthly rent + status pill per row, full width. Selected-tenants panel gets `max-height: 520 px + overflow-y: auto` so the page stays stable when many tenants are added. | — | App-port: do **not** add `lockin_months` / `maintenance_paise` `escalation_pct` / `notes` to the `leases` table — earlier ledger row to be ignored. Late fee % and notice-period days live in a platform `settings` table. Maintenance charges DO stay (per-lease — `monthly_maintenance_paise BIGINT`). |
| 2026-05-30 | Admin · Create Lease | **History grid moved below the wizard action bar + wrapped in a card.** Reference content lives at `id="step5-history-card"` outside the wizard panel; visibility toggled in `showStep()` (only Step 5). **Combined Unit Lease History** — unit-wise + room-wise leases collapsed into one card with per-row scope chips (`Unit-wise` / `Room A` / …) sorted by start date desc. Tenants with zero history are skipped entirely (no empty rows). **Bug fix**: `UNIT_LEASES` had no entries for `gv-1A`, `gv-2B`, `pg202-C` (units showed Occupied but had no lease record) — rows added matching `occupiedUntil`. Two date mismatches corrected (`gv-5B-A`, `pg101-B`). | — | App-port: the eventual `GET /units/:id/leases` endpoint returns both unit-wise and room-wise leases unioned with a `scope` field; client renders the chip from that. |
| 2026-05-30 | Admin · Create Lease | **Wizard step indicator stretched to card width.** `.wizard-progress` width:100%; `.wizard-connector` flex:1 1 0 with min-width:16px so connectors absorb available horizontal space on wide screens. | — | Cosmetic. |
| 2026-05-30 | Admin · Create Lease | **Step 5 context strip** — new `.step5-context-strip` at the top of the panel: saffron-tinted card with a `UNIT-WISE LEASE` / `ROOM-WISE LEASE` chip + breadcrumb `Property › Unit (› Room)`. Required because the summary rail is intentionally hidden on Step 5 (the history card claims full width), and without it the user had no on-screen reminder of *what* lease they were configuring. Populated by `renderStep5ContextStrip()` called from `prepareStep5()` so it works for both manual entry and the deep-link path. `aria-live="polite"`. | — | Frontend-only. |
| 2026-05-30 | Prototype mock data · CLAUDE.md rule #19 | **Numeric-id refactor of `create-lease.html` mock data** and **CLAUDE.md rule #19 strengthened**. (1) Rule #19 now binds cross-table relations and prototype mock data: every relation references numeric `id` only, never slug / email / name / any other column. Prisma `@relation` always at the integer PK. (2) `PROPERTIES` got `id: 1..4` + secondary `slug`; `UNITS` flattened to one array with numeric `id` + numeric `propertyId`; `ROOMS` flattened to one array with numeric `id` + numeric `unitId`; `TENANTS` numeric `id: 1..5`; `UNIT_LEASES` flat with numeric `unitId` / `roomId` / `tenantId`. (3) Helper lookups added: `findPropertyById`, `findUnitById`, `findRoomById`, `findTenantById`, `unitsForProperty`, `roomsForUnit`, `leasesForUnit`, `unitWiseLeasesForUnit`, `leasesForRoom`. (4) ~20 onclick handlers updated to pass numeric ids: `selectProperty(1)`, `selectUnit(105)`, `selectRoom(10501)`, `selectExistingTenant(idx, 3)`. (5) `dataset` string-coercion bug fixed via `Number()` in `findTenantById` and `renderTenantDropdown`. (6) Step 2 stale-grid bug fixed — `selectLeaseType()` re-renders the property grid only when the type actually changes. | — | App-port: this is the binding pattern. Every entity gets `id INTEGER AUTO_INCREMENT` PK; slugs are secondary `VARCHAR UNIQUE` only; every Prisma `@relation` references the numeric id. Mock-data IDs (`1..4` for properties, `101..401` for units, `10501..20203` for rooms) can be regenerated from `serial` sequences. |
| 2026-05-30 | Admin · Unit detail · Quick-create-lease | **Per-unit and per-room deep-link buttons added.** Top of admin unit-detail gains a `+ Create Lease for this Unit` button (`create-lease.html?unitId=<n>`); each row in the Rooms table gets a `+ Create Lease` button (`create-lease.html?unitId=<n>&roomId=<m>`). The wizard reads the URL params on load, validates the room/unit relationship, seeds `wizardState`, and jumps the user directly to Step 4 (Tenants) with property/unit/(room) pre-selected and the lease type already set to unit-wise or room-wise. **PM unit-detail intentionally not given this affordance** — cross-folder link to `../admin/create-lease.html` would render the wizard with the Admin sidebar around it, which is wrong for a PM user; a `pm/create-lease.html` mirror page would be needed first. Placeholder comment added explaining the deferral. | — | App-port: wizard reads URL params via `useSearchParams()`; both `/admin/units/[id]` and `/admin/units/[id]/rooms/[roomId]` pages emit the link. PM mirror is a v2 candidate. |
| 2026-05-30 | All roles · Date inputs · NEW `assets/date-picker.js` + bulk rollout | **All DD/MM/YYYY date inputs in the prototype are now click-to-open calendar widgets.** New `prototype/assets/date-picker.js` (vanilla JS, no deps) — progressive enhancement on `<input data-datepicker>`. Mon-start week (Indian convention). Supports `data-min-date="today"|D/M/Y|YYYY-MM-DD`, `data-max-date=…`, and **range-pair** attributes `data-pair-min="<id>"` / `data-pair-max="<id>"` so an End picker's min auto-clamps to the partner Start's value (and vice-versa). Picker fires `input` + `change` on selection so existing handlers (conflict checks, summary updates, validation) run naturally. Public API: `window.GsDatePicker.initAll(root?)` + `.refresh(root?)` (idempotent — already-enhanced inputs are skipped, so dynamic modals can call refresh after insertion). CSS appended to `assets/styles.css` (`.gs-dp-wrap`/`-pop`/`-header`/`-nav`/`-grid`/`-dow`/`-day` + states + mobile bottom-sheet at max-width:480px). **Bulk rollout: 12 prototype files touched, 20 inputs enhanced**, constraint table applied (`cl-start`, `cl-end`, `rl-start`, `rl-end`, `delegStart`, `delegEnd`, `rent-schedule-effective-date` → `data-min-date="today"`; `ap-date`, `paydate`, `paid-on-date`, `sm-date` → `data-max-date="today"`). **5 date-range pairs wired**: `cl-start↔cl-end` (create-lease), `start↔end` (pm/leases), `from-date↔to-date` (audit-log), `delegStart↔delegEnd` (delegation-new), `rl-start↔rl-end` (pm/lease-detail). | — | App-port: replace with a React date picker (e.g. `react-day-picker`) sharing the same constraint contract — `min/max` props mapped from the same DD/MM/YYYY rules; a `useDateRange()` hook unifies the pair logic. Format / week-start / locale stay `en-IN` + DD/MM/YYYY everywhere. |
| 2026-06-01 | Public · all 8 pages | **Public-pages redesign v2 — glassmorphism / light-forward** (from client Stitch kit `stitch_modern_design_overhaul/`). Hybrid: adopt the new layout + glass + cool-blue light surfaces, rendered in GharSetu's Poppins/Inter + navy/royal-blue/saffron tokens (no Manrope/Hanken, no Material-3 names, no Material-Symbols font — icons inline SVG). New cool-blue **surface tokens** added to `assets/styles.css` `:root` (`--surface`/`--surface-low`/`--surface-container`/`--surface-high`/`--surface-dim`/`--surface-deep`) so the single-source rule holds. `.auth-shell`+`.auth-card` flipped navy→light glass (covers login/forgot/reset/signup); `.public-band` repurposed to a light centered header; `.glass-panel` frosted card added. Homepage rebuilt as bento/glass (Rent·Maintenance·Leases·Visitors bento, navy 3-step band, 5-persona grid incl. Security Guard, navy final-CTA) — plans still via `renderMarketingPlans`. Login gains a Preview-as-Role modal (all 6 roles). Contact = centered title + glass form + Email/Call/Office info. Privacy/Terms = light header + `.glass-panel` content (legal copy still from `legal.js`). Validation contract, DD/MM/YYYY, ₹ grouping, single 1023px breakpoint, WCAG AA all held; no external image hosts. | [`2026-06-01-public-pages-redesign-v2.md`](./features/2026-06-01-public-pages-redesign-v2.md) | Port to `apps/web` `(public)` route group. Fold the new `--surface-*` tokens into `tailwind.config.ts` verbatim (Scope rule I). Glass utilities are landing/auth-only; keep CSS mocks (no external images). Pricing stays one source (`plans.js` → shared catalogue). |
| 2026-06-01 | Admin · Rent · `rent.html` + NEW `record-payment.html` | **Record Payment popup → full page + late-fee allocation + listing cleanup.** (1) NEW `admin/record-payment.html` — full-page Record Payment (Admin-only): searchable lease-select, room, amount, date, method-aware reference, notes; live **Lease summary card** (rent · status · rooms · next due · auto-computed **late fee** at 2%/overdue month · outstanding); full-width **Previous Payments** history table; **Payment Allocation** table below Notes that allocates the entered amount oldest-due-first (rent + late fee per month), spills surplus into a new future month as advance, and labels each month Covered/Partial/Unpaid/Advance — so the amount clearly reflects *which month* it is recorded against. (2) On submit it stashes a success message in `sessionStorage` and **redirects to `rent.html`**, which reads it on load to show the success banner + `gsToast`. (3) `rent.html`: header `+ Record Payment` now links to the full page (modal removed); **per-row "Record" action links removed from the listing table** (20 rows) — the header button is the single entry point. (4) Fixed a column-misalignment bug: the main rent table was missing its `Room` header while the "Overdue tenants" table had a stray `Room` header — headers realigned (main table now 9 ths = 9 tds). (5) **Removed the "Overdue tenants — May 2026" section** entirely — the KPI/filter tiles already surface overdue on click, so the standalone table was duplicate. (6) **Removed the "All units — May 2026" section heading** — the section leads straight into the filter tiles + rent table (no redundant title). | — | App-port: Record Payment is a route (`/admin/rent/record`), not a modal. Allocation logic (oldest-due-first, late fee = Settings rate × overdue months, surplus → advance) is server-computed and previewed client-side. Payments stay record-only (no edit/adjust). Success state via redirect + flash message. |
| 2026-06-01 | Admin · Rent · `rent.html` filters | **Property → Unit → Room filters are now dependent (cascading).** Previously all three dropdowns showed the full catalogue regardless of each other. Added a cascade script that builds membership maps (`property → its units`, `unit → its rooms`) **from the table rows themselves** (single source of truth), then: selecting a Property narrows the Unit list to that property's units (and the Room list to rooms within it); selecting a Unit narrows the Room list to that unit's rooms. A selection is preserved if still valid after re-scope, else falls back to "All …", and the matching Paginator attr-filter is re-applied (a programmatic `value` change doesn't fire the select's own `onchange`). Uses `SearchableSelect.refresh()` to re-sync the two `data-searchable` comboboxes (Property, Unit) after their options change. Also replaced the duplicate **Partial/Prepaid KPI cards** (already present as filter tiles) with **Expected** (₹15,00,000 · 120 units billed) and **Late Fees** (₹6,800 · accrued, auto-calculated). | — | App-port: cascade is driven by the same relational data (property→units→rooms via numeric ids per rule #19); FE derives option scope from the fetched rows or a dedicated `/units?propertyId=` lookup. Keep the "preserve-if-valid-else-All" reset rule. |
| 2026-06-01 | Admin · Rent · `rent.html` Month/period filter | **Month filter added; rent page is now period-aware (cumulative by default).** New **Month** dropdown in the filter card (`All months (cumulative)` default + May→Jan 2026). Selecting a month re-scopes **both** the table rows **and** the 4 KPI cards; the default cumulative view sums all months. To keep the figures honest, **one mock dataset (`ROSTER` of 20 leases × `MONTHS`) now drives both the table body and the KPI cards** — rows are generated by an inline script that runs before `paginate.js` (so the Paginator sees them at init), and the KPIs are *computed* from the same data (Collected = Σ paid, Expected = Σ rent, Overdue = Σ unpaid for overdue rows, Late Fees = 2% × rent for overdue/partial). Only the current month (May 2026) carries arrears; past months model as collected-in-full. Added a **Month column** to the table and a **period label** under the KPIs ("Showing: All months (cumulative)" / "Showing: May 2026"). Verified via DOM-stub harness: 100 rows; cumulative Collected ₹24,40,000 (96%), May-only ₹4,06,000 (80%). The Property→Unit→Room cascade and the status filter tiles continue to work (Paginator ANDs `data-month` with the other attr filters). | — | App-port: period is a query param (`?month=YYYY-MM`, default = cumulative). Server returns the ledger + summary for the scope; FE renders rows + KPI cards from one payload so totals reconcile by construction. Payments stay record-only. |
| 2026-06-01 | Admin · Rent · `rent.html` cumulative redesign | **Fixed the incoherent "cumulative" table.** Previously "All months" listed one row *per tenant per month* (with a Month column + Rent/Paid), which read like running totals but weren't. Now the table **changes shape with the Month filter** so columns always mean what they say: (a) **Cumulative view (default)** = one row **per lease, totalled across all months** — columns `Billed (Σ rent) · Collected (Σ paid) · Outstanding (Billed−Collected, negative = paid in advance) · Standing (Cleared / Due / In advance)`, **no Month column**; status tiles switch to **All / Cleared / Due / Advance**. (b) **Single-month view** = one row per lease for that month — columns `Rent · Paid · Status`, **no Month column** (the filter + period label state the month); tiles **All / Paid / Partial / Overdue / Pre-paid**. The header, rows, and tiles are all re-rendered by `render(scope)` on month change; since `paginate()` re-reads `tbody.rows` each call, a `Paginator.setFilter(...,'all',...)` after re-render recomputes pagination + tile counts against the fresh rows, and the cascade's property/unit/room attr-filters persist automatically. KPI cards still computed from the same `ROSTER × MONTHS` dataset, so the cumulative Billed/Collected column totals equal the Expected/Collected KPI cards by construction (₹25,42,500 / ₹24,40,000). Verified via DOM-stub: cumulative 10 cols/20 rows, month views 9 cols/20 rows; Vikram Mehta → Outstanding ₹12,000 Due; Vikram Singh → –₹6,000 In advance. | — | App-port: two server projections behind `?month=` — a cumulative aggregate (group-by lease, SUM billed/collected) and a per-month slice. Standing derived server-side; tiles + columns swap on the client per projection. |
| 2026-06-01 | Admin · Rent · Lease-wise / Property-wise tabs | **Two view tabs on the rent page (view-only).** New segmented control (`.view-tabs`/`.view-tab` added to `styles.css` — navy active / royal-blue idle) above the table: **Lease-wise** (default) and **Property-wise**. A **shared Month filter** moved up beside the tabs (always visible); both views are **cumulative by default** and re-scope to a single month. Lease-wise keeps the existing per-lease behaviour + the Tenant/Property/Unit/Room/PM filter card + cascade. Property-wise **aggregates leases by property** (`# · Property · Units · Billed · Collected · Outstanding · Standing`; tiles All/Cleared/Due/Advance) — the lease-only filter card is hidden here. `render(tab, scope)` rebuilds header+rows+tiles; switching tabs resets the lease filters (via a `change` dispatch on the property select so the cascade rebuilds) so stale `data-unit/room/pm` constraints can't hide property rows. Month persists across tabs. KPI cards unchanged (org-wide, driven by Month). Verified via DOM-stub: Lease cumulative 20 rows; Property cumulative 4 rows (Green Valley 6 units, ₹6,35,000 / ₹5,95,000 / ₹40,000 Due); Property May ₹1,27,000 / ₹87,000 / ₹40,000. | [`2026-06-01-rent-views-and-lease-collection-schedule.md`](./features/2026-06-01-rent-views-and-lease-collection-schedule.md) | App-port: two server projections behind `?view=lease|property&month=`; group-by-property SUM for the property view. Both views read-only. |
| 2026-06-01 | Admin · Lease detail · Rent Collection schedule | **Month-by-month collection table on the lease detail page** (Block 4b, after Rent Change Schedule; `data-status-show="active,expired,terminated"` — hidden for an Upcoming lease). Lists **every month from lease start → end** with columns **Month · Rent · Collected · Late Fee · Status**. Rent honours the rent-change schedule (₹17,000 → ₹18,000 from 01/05/2026 → ₹19,500 from 01/04/2027); status is relative to the prototype clock (Jun 2026): past = **Received** (collected = rent), current = **Due**, future = **Upcoming**; the **Late Fee is its own column** (₹360 on the one late month, — elsewhere). A footer row totals the lease: **₹2,34,500 billed / ₹35,000 collected / ₹360 late**. Generated by a self-contained inline script (13 rows Apr 2026→Apr 2027). | [`2026-06-01-rent-views-and-lease-collection-schedule.md`](./features/2026-06-01-rent-views-and-lease-collection-schedule.md) | App-port: server returns the lease's billing periods (honouring rent-change effective dates) + per-period collection state; late fee = 2% × overdue weeks per BL-05. View-only. |
| 2026-06-01 | Admin · Leases ↔ Rent ↔ Lease-detail data reconciliation | **Fixed cross-screen data drift (client-demo blocker).** The Leases page (8 hand-typed rows) and Rent page (20 fabricated rows) were unrelated mock sets — same tenant showed different unit/rent on each, and the rent roll listed *more* leases than existed. Reconciled into one canonical dataset: (1) `admin/leases.html` now renders from a `LEASES` array of **24 leases** — the **20 ACTIVE leases are the exact same set the Rent page bills** (same tenant · property · unit · rent), plus 2 Upcoming, 1 Ended, 1 Terminated that exist only on the Leases page. Rows generated before `paginate.js` so the Paginator/filters/tiles see them. (2) `admin/rent.html` ROSTER unchanged in values (already the canonical active set) — added a comment noting the 1:1 mirror (L-2101..L-2120). (3) `admin/lease-detail.html` re-pointed to a real lease in the set — **L-2114 Raj Sharma + Priya, Green Valley Unit 3A, ₹18,000** (was a Rohan-Mehta/Unit-3A/₹18,000 hybrid that contradicted both lists): title, renew link, both tenant cards, and the termination consent table updated. Now: rent roll (20) = active subset of all leases (24); every tenant's unit/rent is identical across screens; the detail page matches its list row. Verified via DOM-stub: Leases 24 rows (20 active/2 upcoming/1 ended/1 terminated), Rohan Mehta = 7C ₹22,000 on both, L-2114 = Raj Sharma + Priya 3A ₹18,000. | — | App-port: this is the real relation — `leases` table holds all leases; the rent roll is `WHERE status='active'` joined to the period's payments. One source, two views. NOTE: other pages (dashboard, property/unit detail, PM pages) may still carry independent mock tenants — reconcile in a later pass if needed. |
| 2026-06-01 | Admin · Leases · default to Active | **Lease list now defaults to Active leases only.** The status filter tiles (All / Upcoming / Active / Ended / Terminated) already existed; added a small post-`paginate.js` script that sets the default tile filter to **Active** on load (so inactive/closed leases — Upcoming, Ended, Terminated — are hidden until the user clicks their tile). A shared deep-link (`?tbl-leases_f=…`) is honoured, so an explicitly-linked status still wins. | — | App-port: default the leases query to `status=active`; status chips/segmented filter toggle it. |
| 2026-06-01 | Admin · Rent · filters + combined cell | **Rent page filter & column refinements.** (1) **Property · Unit · Room combined into one bold lead column** on the Lease-wise views (cumulative + monthly): the cell shows the property name (bold) with `Unit X · Room Y` beneath — the lease is the record, so it leads; the **Tenant moves to a normal-weight column** after it (per `property-unit-combined-cell.md` + `no-helper-caption`). Headers now `# · Property · Unit · Tenant · PM · …`. (2) **Lease type filter** added to the filter card (Unit-wise / Room-wise) — rows carry `data-lease-type` (derived: room if the lease has a room, else unit); cleared on tab-switch so it can't blank the Property-wise view. (3) **Payment status dropdown** added to the filter card, mirroring the tiles and kept in sync via a single `rentSetStatus()` entry point (tiles + dropdown both call it). Options re-render per view (cumulative: Cleared/Due/Advance · month: Paid/Partial/Overdue/Pre-paid). **Labelled "Payment status"** (not "Status") to make clear it is collection status, not lease status — lease-status filtering lives on the Leases page (rent roll = active leases only). (4) Confirmed the Property→Unit→Room cascade is intact (narrows by row membership; the reconciled roster has a single room-wise lease, so the Room list rarely changes — expected). | — | App-port: combined cell is one column bound to the lease's property/unit/room; tenant is secondary. Payment-status filter = `?payment_status=`; lease-status filter stays on the leases list. |
| 2026-06-01 | Admin · Rent · Lease status filter (replaces Payment status) | Per user: **removed the Payment-status dropdown** from the Rent filter card and **added a Lease status filter** (All / Active / Upcoming / Ended / Terminated) — `data-lease-status` on rows, `Paginator.setAttrFilter('data-lease-status', …)`, cleared on tab-switch. Reverted the tile↔dropdown sync machinery (tiles back to plain `Paginator.setFilter`). The payment-standing tiles above the table remain. | — | App-port: lease-status is a row attr/filter; payment standing stays the tile filter. |
| 2026-06-01 | Admin · Rent · Lease status filter now has data | The Lease status filter showed nothing for Upcoming/Ended/Terminated because the rent ROSTER held only active leases. Added the 4 non-active leases (same as `leases.html`: Aditi Joshi/Sunil Kapoor upcoming, Pradeep Yadav ended, Karan Malhotra terminated) with a `ls` field. Non-active rows render their lease-status badge + "—" in the rent columns (not billed) and are excluded from the KPI / property-total sums (rent roll = active only). Rent table now = 24 leases (20 active / 2 upcoming / 1 ended / 1 terminated), matching the Leases page; the Lease status filter returns rows for every value. | — | App-port: rent view can list all leases with status; financial columns/KPIs scoped to active billing. |
| 2026-06-01 | Admin · Dashboard · Lease Expirations table | **Lease Expirations — Next 60 Days rows now link to lease detail.** Each row's Lease ID cell (`#LS-1021`, `#LS-1044`, `#LS-1057`, `#LS-1063`) wrapped in a `text-royal-blue` anchor → `lease-detail.html?id=<id>`, matching the Leases-page convention. No whole-row click (no such pattern exists in the prototype). NOTE: these dashboard lease codes/dates are NOT reconciled with the canonical 24-lease roster (`leases.html`) — a later pass could align them. | — | App-port: row links to `/admin/leases/[id]`; dashboard expirations should query the same lease table as the roster. |
| 2026-06-01 | Admin · Properties · room availability tiles | **Two room tiles added to the portfolio summary**, mirroring the existing "Available Units" tile: **Available Rooms** (8 → links to `create-lease.html` to start a room-wise lease) and **Total Rooms** (30 = 5 independent buildings × 6 rooms; a `<button>` that sets the Room-status filter to "Has rooms" via `Paginator.setAttrFilter`). Tile row: Available Units · Available Rooms · Total Rooms · Total Active Leases · Upcoming Leases. Counts are mock (not cross-checked vs a room-lease roster). | — | App-port: tiles bound to room-availability aggregates; Total Rooms deep-links to a rooms filter. |
| 2026-06-01 | Admin + PM · Upcoming lease date refresh | **Upcoming lease L-2245 (Aditi Joshi, Green Valley 1B) moved from a past start to a genuine future term: 01/04/2026 → 31/03/2027 became 01/07/2026 → 30/06/2027** (today = 01/06/2026, so the old start was in the past). Updated in 4 files: `admin/unit-detail.html` + `pm/unit-detail.html` (displayed Leases table) and `admin/create-lease.html` + `pm/create-lease.html` (wizard tenant-history + `UNIT_LEASES` mock arrays, 2 rows each). Leases/Rent tables show no dates so were untouched. NOTE (not changed): L-2204 upcoming mock starts 01/06/2026 (today); the same unit's "active" L-2103 ends 31/03/2026 (already past) — deeper roster-date reconciliation deferred. | — | App-port: dates are real lease records; "upcoming" = `start_date > today`. |
| 2026-06-01 | Admin · Leases · Termination + Unit filters, consent section removed | **(1) Termination-requested (Yes/No) filter** added to the filter card — rows carry `data-termination` (`Paginator.setAttrFilter`); two leases flagged "yes": L-2114 (active co-tenant lease, pending consent) and L-2080 (terminated). Page defaults to the Active tile, so "Yes" surfaces L-2114; switch to All to also see the terminated one (filters compose). **(2) Unit filter** added as a searchable combobox (rule #18, `data-searchable`); options auto-populated from the lease roster (distinct unit codes, natural-sorted) inside the roster IIFE — before `searchable-select.js` initialises. Filter grid 3→5 fields. **(3) Removed the "Pending Co-Tenant Consent" section** entirely (it referenced a phantom lease not in the roster). | — | App-port: termination filter = `?has_termination_request=`; unit filter = `?unitId=`. The consent panel belongs on the lease-detail / termination flow, not the list. |
| 2026-06-01 | Admin · Users · action links → buttons, Impersonate last | **All 29 row action cells converted from plain text links to distinct buttons** (compact `btn … !py-1 !px-3 !text-sm` pattern from visitors.html): **Edit/Reset** = `btn-secondary` (blue outline), **Deactivate** = `btn-danger` (red) / **Activate** = `btn-secondary`, **Impersonate** = `btn-primary` (saffron) and **moved to be the last button**. `·` separators dropped; buttons wrapped in `flex flex-wrap items-center gap-2`. Impersonate keeps its `imper-link` class + `data-target-*` attrs + hidden state on inactive rows. JS patched: `confirmStatusToggle` now finds the status control tag-agnostically (`[onclick*="openStatusToggle"]`) and swaps `btn-danger`↔`btn-secondary` instead of a `.danger` class. | — | App-port: actions are buttons; Impersonate gated by NR-7 (PM / Maintenance / Tenant / Security only, never Admin / Super Admin) and rendered last. |
| 2026-06-01 | Admin · Properties · Occupancy column removed + edit-form bug fix | **(1) Removed the Occupancy column** (header + 14 row cells). Columns now: # · Property · Address · Type · Units · Rooms · Manager · (actions). **(2) Fixed "edit form shows incorrect data"** — `editPropertyRow` read cells off-by-one because it ignored the leading `<td data-pg-serial>` cell (it was loading the serial number into Name, Name into Address, etc.). Re-indexed to c[1]=Property, c[2]=Address, c[3]=Type, c[4]=Units, c[6]=Manager. **(3) Fixed CSV export** which had the same off-by-one (started at `tds[0]`) and omitted Rooms — now maps explicit cells [1..6] and drops Occupancy. | — | App-port: edit form binds to the property record by id, not by scraping table cells; export columns match the visible table. |
| 2026-06-01 | Admin · Delegations · dedicated Detail page (replaces side-drawer) | **NEW `admin/delegation-detail.html`** — full page replacing the right-side View drawer. Detail **card** (delegate name + role chip + status badge; Tasks granted chips · Window `start → end · N days` + "N days remaining" for Active · Created by · **Revoked by** only when revoked) and below it an **"Actions taken during this window"** table (When (IST) · Action badge · Target) with a real empty state (Pooja/upcoming = none). Reads `?id=<n>` from a `DELEGATIONS` array keyed by **numeric ids 1–7** (rule #19). On `delegations.html`: 7 row **View** links rewired to `delegation-detail.html?id=<n>`; the side-drawer markup + `.detail-drawer*` CSS + `drawerData`/`openDetailDrawer`/`closeDetailDrawer` JS removed; Escape handler trimmed to the revoke modal. Also: **Revoked tab "Revoked by" column removed** (that data now lives on the detail card). **Decision (user): the global `delegation-activity.html` was DELETED** — one page to maintain, not two; the per-delegation page is the only actions view. | [`2026-06-01-delegation-detail-page.md`](./features/2026-06-01-delegation-detail-page.md) | App-port: route `/admin/delegations/[id]`; detail card from the `delegations` record; actions table = `audit_log WHERE delegation_id = :id AND created_at ∈ [start,end]`, attributed to the delegate (NR-8). Window scoping enforced server-side. |
| 2026-06-01 | Admin · Delegation Detail · clearer actions + sidebar fix | **(1) Actions table made readable.** Each action now has a friendly **label** ("Rent changed", "Payment recorded", "Request raised"…), the raw audit **code** shown small/mono beneath it, and a plain-English **"What changed"** sentence with specifics (amounts, ₹before→₹after, tenant·unit·property, priority, resolution, reason). Columns: When (IST) · Action · What changed. **(2) Fixed the sidebar Master Data header** rendering a giant icon/chevron — the page had used `sidebar-section-title`/`sidebar-section-chev` (unsized by CSS); replaced with the canonical `.section-icon` + `.section-chev` markup from `delegations.html`. | [`2026-06-01-delegation-detail-page.md`](./features/2026-06-01-delegation-detail-page.md) | App-port: the human label + the audit code both come from the `audit_log` action enum (code = stored value, label = display map); the "What changed" sentence is composed server-side from the entry's structured fields. |
| 2026-06-01 | Admin Properties + PM Dashboard · lease deep-link param fix | **Fixed "Upcoming Leases" tile landing on the default lease view.** The tiles linked `leases.html?filter=upcoming`, but the Paginator reads the deep-link param as `<tableId>_f` → `tbl-leases_f`; the wrong param was ignored so the page fell back to its Active default. Corrected 3 links to `?tbl-leases_f=upcoming` / `=active`: `admin/properties.html` (Total Active Leases + Upcoming Leases tiles) and `pm/dashboard.html` (Upcoming KPI card). Now the Leases page opens with the matching status tab active + rows filtered. NOTE: other dashboard `?filter=` deep-links (maintenance/properties/rent KPIs) use an inconsistent convention and may not apply their filter — `super-admin/organizations.html?filter=pending` works (custom handler). Deferred a full audit. | — | App-port: deep-links should use one shared query convention (e.g. `?status=upcoming`) read by every list page; don't mix `?filter=` with the Paginator's `_f`. |
| 2026-06-02 | Admin · all pages · sidebar nav reorder by priority | Reordered the admin sidebar + mobile more-sheet across **all 28 admin pages** by module priority — **Dashboard → Properties → Leases → Rent collection → Maintenance → Visitors → Users → Delegations → Master Data → Settings → Organization → Audit Log** (Audit Log moved from mid-list to the end). Bottom tabbar unchanged; no hrefs/SVGs/labels/`active` classes altered. Done via a one-off idempotent Node transform (`scripts/reorder-admin-nav.js`) that was **deleted afterwards** (not committed). | — | App-port: encode nav order once in a shared `adminNav` config consumed by the sidebar + more-sheet components; don't hand-order per page. |
| 2026-06-02 | Admin · Lease detail · Security Deposit section (NEW) | New **Security Deposit** block (deposit ₹36,000; KPIs total/refunded/balance; refunds table + empty state) with a **Record Deposit Refund** modal (Full/Partial toggle, amount ≤ balance, method from `payment-methods.js` with method-aware reference honouring `refRequired`, date, note; button locks to "Fully refunded" at ₹0). **Refund gated to ended/terminated leases** (`data-status-show="expired,terminated"`; an explanatory note shows for upcoming/active). **Rent Change Schedule + Security Deposit now side-by-side 6:6**; Rent Collection stays full-width below. | [`2026-06-01-...`](./features/) (lease-detail) | App-port: `lease_deposits` (amount) + `deposit_refunds` (amount, method, reference, date, note, type full/partial) append-only; refund allowed only when `lease.status ∈ {ended, terminated}`; balance = deposit − Σ refunds; record-only (no edit), BL-10 actor scope. |
| 2026-06-02 | Admin · Lease detail · Tenants merged into Lease Summary | Removed the separate Tenants card; **Lease Summary is now a 2-col card** — left = summary rows (incl. status badge), right = Tenants in a **fixed-height scroll** container (`max-height:340px; overflow-y:auto`) so card height stays constant as co-tenants grow. | — | App-port: cosmetic; tenants come from the lease's co-tenant set. |
| 2026-06-02 | Admin · Create Lease · renew deep-link fix | `create-lease.html?renew=2114` was falling back to the default step — the `RENEWALS` lookup only had key `'2103'` while the lease-id reconciliation had re-keyed the renewable lease to `2114`. Added the `'2114'` entry (unit 103, 2 co-tenants, ₹18,000 / ₹36,000 deposit). Renew mode now lands on the **Tenants step** with the title swapped to "Renew Lease". | — | App-port: renew flow keys off the real `lease_id`; no hard-coded map. |
| 2026-06-02 | Admin · Settings · trim + same-page save + button redesign | Removed the read-only "last changed by …" line, the standalone **Cancel** button (+ dead `cancelSettings`/`backToSettings`), and the "Full history in the Audit Log" link. **Save stays on the same page** (toast + Recent-changes table) instead of swapping to a success card; removed the duplicate "Saving…" toast (one success toast). **Save button redesigned** — `btn-primary` when dirty, save icon + `#saveBtnLabel`, min-width 172px. | — | App-port: settings PATCH returns the saved values; the page re-renders inline. Audit history lives on the Audit Log page (no inline link needed). |
| 2026-06-02 | Admin · Organization · property cap + plan-history badge | Subscription Plan History — plan value rendered as a **badge consistently** across all rows (one row was plain text). Added a **Property cap** row (`#sub-property-cap-display`) + Active-properties + a Property-cap column in the history table; JS reads `plan.propertyCap` (null → "Unlimited properties"). | — | App-port: `plans.property_cap` (nullable = unlimited); org Subscription card + history both read it alongside `user_cap`. |
| 2026-06-02 | Admin · Invoice detail · remove GST line | Removed the "GST line item — deferred (see planning note §4 Q2)" row from `admin/invoice-detail.html`. | — | App-port: invoices have no GST line in the current engagement. |
| 2026-06-02 | Admin · Record Payment · period-first model (PLAN ONLY) | Authored the plan to shift Record Payment from **amount-first** to **period-first** (pick the billing month → late fee + amount derive from period + payment date; period stays listed until fully settled; live Payment Summary; 4 context cards). **No code written.** User decisions: plan-first + **keep BL-13 unchanged (no month-end cap)** — the reference screenshot's cap is dropped. Two sign-off questions open (rework-vs-augment; single-period+rollover vs multi-select). | [`2026-06-02-record-payment-period-first.md`](./features/2026-06-02-record-payment-period-first.md) | App-port: server returns per-period `outstanding_rent` + derived BL-13 late fee for a payment date; payment posts against a `period_id` (oldest-first rollover); BL-10/11/12/13 server-enforced; status computed, never stored. |
| 2026-06-02 | Rent collection · period-first model BUILT (all roles) | Implemented the period-first model on a single-source engine. **NEW `assets/rent-engine.js`** (allocator oldest-first, month-end-capped late fee, query-time status + `displayStatus`, unsettled periods, transaction grouping) + **NEW `assets/rent-data.js`** (canonical 8-lease roster, numeric ids, both lease types, all statuses). **`admin/record-payment.html`** reworked amount-first → period-first (context cards · Billing-Period dropdown · live allocation preview · sticky summary · grouped history). **`admin/rent.html`** listing reworked to engine rows (4 scope tiles · By Lease/By Building/Portfolio · combined Property·Unit·Room cell · `displayStatus` badges). **`pm/rent-collection.html`** reworked (PM scope, no Portfolio) + **NEW `pm/record-payment.html`**. **`tenant/rent.html`** reworked (cumulative tiles · per-lease current-month card · merged history · read-only). **Lease type (unit/room) is a display addon only** — rent logic identical. **DECISION: BL-13 now month-end-capped** (~8%/period) — reverses the earlier "no cap"; SRS §5 + Settings copy re-synced. | [`2026-06-02-record-payment-period-first.md`](./features/2026-06-02-record-payment-period-first.md) · [`RENT_COLLECTION_CONTEXT.md`](./RENT_COLLECTION_CONTEXT.md) | App-port: engine maps to `RentService` (allocate/computeRentStatus/computeFeeForPeriod with month-end cap); per-period `rent_payments` rows with `parent_id` grouping + late-fee snapshot; BL-10/11/12/13 server-enforced; status never stored. |
| 2026-06-02 | Security portal · role fix + single-source visitor data + gate card removed | **`security/profile.html`** fixed from a leftover PM identity → Security Guard (Ramesh Gupta · role · email · Assigned gates + Shift · gate-action Recent Activity). **NEW `assets/security-visitors.js`** single source; **`security/visitors.html`** table now renders from it (+ fixed a pre-existing off-by-one in row-action cell indices from the serial cell) and **`security/dashboard.html`** KPIs + awaiting list now computed from the same source so they can't diverge. Removed the "Visitor at the gate?" quick-action card from the dashboard. | — | App-port: `visitor_requests` is the single source; dashboard counts are `GROUP BY status` over the guard's assigned-gate scope; guard profile reads role=SECURITY_GUARD + gate assignments. |
| 2026-06-02 | Rent collection · period-first refinements (all roles) | Post-build refinements on admin + PM + tenant: **late-fee rate/grace fetched from Settings** (NEW `assets/rent-settings.js`, persisted; `admin/settings.html` is the editor; engine reads it live). Record Payment: **Billing-Period pre-fills the cumulative due through the selected month** (oldest settles first, editable); **allocation table** re-columned to Period · Rent applied · Fee applied · Status · **Remaining** + **Total row**; status label **Settled** (was "Covered"); **no payment method selected by default** (+ validation); **"Unallocated" summary row removed**; **`(BL-…)` codes stripped** from all client-facing copy + the audit-footnote removed. Listings: **"Amount Due/Total Due" → "Monthly Rent"**; **"Collected" tile → "this month"**; **"Partial Overdue" → "Partial Paid"**; **Overdue filter now matches the tile** (includes Partial Paid; redundant option dropped); **lease type (Unit/Room-wise) on every row**; **Late Fee column = total accrued fee** (was current-month only); **By-Building "See leases" popup** (was inline) with full building summary header + **current-month badge** + **Collected = this month**; By-Building "Collected" rate fix (was negative); **Portfolio tab removed** (admin mirrors PM). Sidebar label made consistent per role (tenant rent "Rent"). | [`2026-06-02-record-payment-period-first.md`](./features/2026-06-02-record-payment-period-first.md) | App-port: Settings table drives `late_fee_rate_percent`/`grace_days`; UI labels are cosmetic; "collected this month" = `SUM(amount+late_fee) WHERE payment_date ∈ current month`; accrued-fee column = `SUM(fee_outstanding)` over unsettled periods. |
